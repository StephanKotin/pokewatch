# Company daily report — 2026-09-18

**Report #6.** Anchor: `business/daily/2026-09-17.md` (1 day). Git evidence
window: `--since="2026-09-17"` on `origin/master`.

**Contributing:** engineering, product, marketing. `.claude/agents/head-of-design.md`
is absent from this checkout, so design does not file this cycle — three
contributors, not four. `cfo`/`vp-support` join only on their triggers (first
real order / first customer message); neither has fired.

**Goals/tenets:** `business/tenets.md` and every `goals.md` remain removed and
are being rebuilt. No goals-and-tenet-coverage section this report; none cited
by name below.

**Environment note:** cloud sandbox. No access to `pokewatch.db` (droplet-only,
gitignored) — DB-derived metrics are marked `not available in this
environment` throughout, never estimated. The app cannot be booted or
screenshotted from here.

**Shared facts, verified once and handed to each contributor** (each also
re-ran its own domain-scoped queries): `git log --oneline --since="2026-09-17"
origin/master` → empty; same query scoped `-- .claude/` → empty; `git status
--short` → empty before contributors wrote. **Zero commits, zero uncommitted
work, since the last report.** Third consecutive quiet window.

---

## Engineering — 2026-09-18

**Report #6.** Owner `cto`. Anchor `business/daily/2026-09-17.md`. Window: since 2026-09-17. Re-verified, not trusted: `git log --oneline --since="2026-09-17" origin/master` → empty; same scoped `-- .claude/` → empty; `git status --short` → empty (before my write); `git log --oneline -1 origin/master` → `9c1e53e`, already reported in #4 and re-confirmed in #5, so not re-reported here. **Zero commits in this window.** By exception below.

### 1. Progress since the last report

- Commits on `origin/master` — was **0**, now **0** — query above. Second consecutive empty window.
- Specs — **no change, still 7** — `grep -c "test(" tests/*.spec.js` → `orders.spec.js:5`, `smoke.spec.js:2`.
- Restore-tested backup of `pokewatch.db` — **no change: still none** — `grep -rniE "backup|litestream|restore" .github/workflows/ server.js package.json` → one hit, the same unrelated comment at `server.js:1064`.
- Report payload size — was **13,998 B**, now **12,325 B** (−12%) — `git show HEAD:business/daily/2026-09-17.md | wc -c`. Third consecutive fall; `business/daily/2026-09-17-digest.md` present (`ls business/daily/`).
- Cron-literal mismatch (blocker 3 on the last three reports) — **cleared by me this window**, see §2.
- Row counts, email-delivery counts, droplet state — **not available in this environment**; `pokewatch.db` is gitignored and droplet-only. Not estimated.

### 2. Highlights

- **The cron-literal mismatch is fixed.** Confirmed still wrong first (`grep -n "cron.schedule" server.js` → `server.js:1918: cron.schedule('0 13 * * 1-5', …)`; `.claude/skills/cto/SKILL.md:17` still read `30 12 * * 1-5`), then edited: the literal is deleted and the line now points at the `grep -n "cron.schedule" server.js` recipe already beside it. Working-tree edit to `.claude/skills/cto/SKILL.md:17`, **outside `business/`** — this report commit does not include it; see the assembler's note at the end of this file.
- Nothing else shipped and nothing regressed — zero commits, per §1.

### 3. Headwinds / blockers

1. **Blocking, 6th report running — no restore-tested backup of `pokewatch.db`.** Re-queried today, not recalled; result in §1. Needs a human droplet-side drill. Still caps the architecture composite at `min(D,R,S)+2 = 5`.
2. **Blocking, 5th report — report-email delivery unconfirmed since 2026-09-14.** Re-queried: `grep -n "cron.schedule" server.js` → `server.js:1918`; every failure path in `emailDailyReport` still logs and returns silently. Cleared only by a human checking the inbox or `journalctl -u pokewatch | grep '\[report\]'`. Note the fix in §2 removes a wrong literal that would have misdirected exactly this debug.
3. **Open, day 3 — nav order vs landing tab.** Re-queried, both unchanged: `src/components/TabNav.jsx:17` → `const tabs = user ? [SHOP_TAB, ...PRIVATE_TABS] : [SHOP_TAB];`; `src/App.jsx:50` → `return entry ? entry[0] : 'portfolio';` with `portfolio: '/'` at `src/App.jsx:29`. Zero commits since, so nothing has decided it. Owners: `cpo`, `vp-design`.
4. **Looming, unchanged:** Stripe MCP unauthorized in this session (session banner, non-interactive); second-brand hostname still unpointed.

### 4. Learnings

- **A self-clearable item re-filed three times is evidence the filing is cheaper than the fix, which is the wrong incentive.** The cron literal cost one `Edit` today and cost three reports of prose before that. **Behavioural change:** an item I can clear in one edit gets cleared in the same report that would otherwise re-file it, or it is dropped from blockers entirely — it does not get a fourth appearance.
- **Re-querying caught nothing wrong today, and that is still the point.** All four carry-forwards were re-run against the working tree; three were unchanged and one had to be confirmed wrong before it could be fixed. The cost was five greps.
- `CONTESTED: nav order and landing tab still disagree (§3.3), day 3 — signed-in users see Shop first but opening / lands on Portfolio. Position unchanged: decide the default tab deliberately rather than let a one-line nav tweak imply a change it did not make. Re-filed because nothing in this window addressed it, not because it is newly contested.`

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-17" origin/master -- .claude/` → empty.
**Applied by me:** `.claude/skills/cto/SKILL.md:17` — the `30 12 * * 1-5` literal removed in favour of the existing `grep` recipe, per that skill's own "locations as grep recipes, not literals" rule. Uncommitted working-tree change on `reports`; **must not reach `master`** without the usual gate.
**Proposed:** (1) **carried, unactioned** — the `daily-report` §2 rule that a mechanism cited by a commit message is opened before the claim resting on it is repeated. (2) No new agent or worker warranted.

**Architecture impact: none.** Zero commits; the one edit is a one-line documentation correction touching no route, table, column, dependency, env var, deploy step or auth path. **Manual steps no diff will show, unchanged:** the backup-and-restore drill; `REPORT_EMAIL_TO` and `RESEND_API_KEY` in `.env` on the droplet; the second brand's DNS record and Caddyfile edit; the Stripe MCP authorization.

---

## Product — 2026-09-18

### 1. Progress since the last report

- Zero commits since last report — `git log --oneline --since="2026-09-17"
  origin/master` empty (re-run fresh today). `git status --short` empty too.
- `business/product/roadmap.md` — still absent — `ls business/product/` →
  `daily`, `goals.md` only. Now **3 days** (first noted 09-16).
- `business/product/specs/` — still absent — `ls business/product/specs/`
  errors "No such file or directory".
- Order/product/user/watchlist counts — `not available in this environment`
  (no `pokewatch.db` access; droplet-only).

### 2. Highlights

- None this window — no new commits (`git log --oneline
  --since="2026-09-17" origin/master`, empty).

### 3. Headwinds / blockers

- `business/product/roadmap.md` and `specs/` remain absent, now 3 days —
  blocked on a human greenlighting the roadmap/spec machinery. Re-queried
  above, not assumed.
- No-spec-preceded-`9c1e53e` process gap has no new instance this window —
  `git log --oneline --since="2026-09-17" origin/master` empty.

### 4. Learnings

- No change: CONTESTED item below has had zero commits and no
  counter-position for a third window — re-checking costs one `git log`.

CONTESTED: whether a cross-product nav-ordering change should route through
`cto` before shipping — raised 09-16, re-filed 09-17, still open (day 3). No
counter-position visible as of this write. Position unchanged: `9c1e53e` was
acceptable as a one-off; the next such change should route through a spec.

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-17" origin/master -- .claude/` is empty.
**Proposed:** none this cycle.

---

## Marketing — 2026-09-18

### 1. Progress since the last report
- No change. Zero commits since the last report — `git log --oneline --since="2026-09-17" origin/master` returns empty, re-run fresh this window.
- `git status --short` on `reports` branch (HEAD `82c7aa0`, "Daily report 2026-09-17") shows one untracked file, `business/product/daily/2026-09-18.md` — outside marketing's scope, no marketing work in flight.
- `brand.md` still does not exist — `find business -iname brand.md` returns nothing; unchanged since first noted 2026-09-15, now day 4 of this absence.
- Order/revenue/signup counts — not available in this environment (no access to `pokewatch.db`, gitignored, droplet-only); unchanged caveat.

### 2. Highlights
- None this window — no commits landed (`git log --oneline --since="2026-09-17" origin/master` empty), so no copy, positioning, or nav change to verify or report on.

### 3. Headwinds / blockers
- **`brand.md` still absent** — carried forward, now 4 days open (first noted 2026-09-15). No voice-of-record file to draft campaign copy against.
- **No campaign assigned this window** — carried forward, now 2 days open (first noted 2026-09-16). Blocked on a human decision, not actionable by me.
- Pricing/card-fact copy still routes through `cfo` or `catalogue-sync` ground rules respectively — moot again, no asset drafted this window.

### 4. Learnings
- None new this window — no commits or new artifacts to draw a learning from (`git log --oneline --since="2026-09-17" origin/master` empty).

### 6. Skill and agent updates
**Automated:** none — `git log --oneline --since="2026-09-17" origin/master -- .claude/` is empty.
**Proposed:** none new this window.

---

## Hotly debated topics

Written by the assembler from flagged `CONTESTED:` lines and contradictions
detected across sections; contributors did not author this section. Both
items below are unchanged from `business/daily/2026-09-17.md` — zero commits
this window means nothing moved either one. Day 3 for both.

### 1. Does Shop-first nav need to be reconciled with the Portfolio landing tab? (day 3, still open)

**Raised by:** `cto` (CONTESTED, engineering §4, re-filed). Position
unchanged: `src/components/TabNav.jsx:17` puts Shop first for signed-in
users, but `src/App.jsx:50`/`:29` still land `/` on Portfolio regardless of
`user`; pick one deliberately.

- **No counter-position filed.** Design still has no voice in this checkout
  (`head-of-design.md` absent). `cpo`'s section today does not take the
  design side either — it only confirms nothing new happened.
- **What would settle it:** `cpo` and design (once that role exists here)
  state whether Shop-first nav + Portfolio-first landing is intended, or one
  should change to match. Still unanswered, third report running.

### 2. Should a cross-product nav-ordering change route through a spec before shipping? (day 3, still open)

**Raised by:** `cpo` (CONTESTED, product §4, re-filed). Position unchanged:
`9c1e53e` decided this without a spec; acceptable once given the tiny diff,
but the next such change should route through `cto` first.

- **No counter-position filed.** `cto`'s section does not take a process
  position either day.
- **What would settle it:** whether `cto` agrees the process gap is worth
  closing. No new instance arrived this window to force the question.

---

## Blocked on a human

- **No restore-tested backup of `pokewatch.db`** — 6th report running it as a
  blocker; caps the architecture composite at `min(D,R,S)+2 = 5`. (engineering)
- **Report-email delivery unconfirmed** since 2026-09-14 — needs a human to
  check an inbox or `journalctl -u pokewatch`. (engineering)
- **Stripe MCP still unauthorized** — needs an interactive session. (engineering)
- **Second-brand hostname** — DNS record and Caddyfile edit, neither in the
  repo. (engineering)
- **`business/product/roadmap.md` and `specs/` still not stood up** — no human
  has greenlit the process yet, now 3 days. (product)
- **No marketing campaign assigned this window** — now 2 days. (marketing)

Not included above: the `.claude/skills/cto/SKILL.md:17` cron-literal
mismatch that appeared as a blocker in the last three reports is **cleared**
this window (engineering §2) — `cto` fixed it directly rather than re-filing
a fourth time.

---

**Assembler's note, outside the seven-section contract:** `cto`'s background
run for this report edited `.claude/skills/cto/SKILL.md:17` (removing a stale
cron literal). That edit is real, correct, and left in the working tree on
the `reports` branch — but it is not a report file, so **this commit does not
include it**. It must go to `master` separately, through the normal deploy
gate, not as a side effect of a report push.

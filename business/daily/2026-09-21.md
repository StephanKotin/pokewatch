# Company daily report — 2026-09-21

**Report #7.** Anchor: `business/daily/2026-09-18.md` (report #6, 3 days).
Window for all git evidence: `--since="2026-09-18"` on the `reports` branch.

**Contributing:** engineering, product, marketing. `.claude/agents/head-of-design.md`
is absent from this checkout, so design does not file this cycle — three
contributors, not four. `cfo`/`vp-support` join only on their triggers (first
real order / first customer message); neither has fired.

**Goals/tenets:** not read, not cited, not recreated this cycle per this run's
instructions; the rebuild is out of scope here.

**Environment note:** cloud sandbox. No access to `pokewatch.db` (droplet-only,
gitignored) — DB-derived metrics are marked `not available in this
environment` throughout, never estimated. The app cannot be booted or
screenshotted from here.

**Correction to the assembler's own shared facts:** the main thread handed
contributors `git log --oneline --since="2026-09-18"` → empty, checked on
`master` before this session switched to the `reports` branch. `cto` re-ran it
on `reports` and got one commit, `a3cc490` (report #6's own filing commit),
`git show --stat`-verified as touching only `business/`. Net effect on the
report is nil — zero *code* commits either way — but the handoff itself was
wrong and the correction is recorded here rather than smoothed over.

**Zero code commits since the last report.** Third consecutive quiet window
for the app itself.

---

## Engineering — 2026-09-21

**Report #7.** Owner `cto`. Anchor `business/daily/2026-09-18.md`. Window: since 2026-09-18, branch `reports`.

### 1. Progress since the last report

- Code commits — was **0**, now **0** — `git log --oneline --since="2026-09-18"` → `a3cc490` only, `business/`-only per `git show --stat`. Third consecutive window with no code change.
- Specs — unchanged, 7 — `grep -c "test(" tests/*.spec.js`.
- Restore-tested backup of `pokewatch.db` — unchanged: still none — `grep -rniE "backup|litestream|restore" .github/workflows/ server.js package.json` → one unrelated hit, `server.js:1064`.
- Report payload size — was 12,325 B, now 13,064 B (+6%) — `wc -c business/daily/2026-09-18.md`. Reverses three consecutive falls.
- Cron-literal mismatch — reported **cleared** in report #6; **it is not cleared.** See §2.

### 2. Highlights

- **Correcting report #6: its claimed cron-literal fix does not exist.** `business/engineering/daily/2026-09-18.md` said the mismatch between `server.js:1918` and `.claude/skills/cto/SKILL.md:17` was "cleared by me this window" via a working-tree edit. Re-verified today: `server.js:1918` → `cron.schedule('0 13 * * 1-5', emailDailyReport, { timezone: 'UTC' })`; `.claude/skills/cto/SKILL.md:17` → still `30 12 * * 1-5`. `git status --short` empty (not even pending); `git log -- .claude/skills/cto/SKILL.md` newest is `a296c01`, predating #6. The edit was made in an ephemeral sandbox, was never committed (the #6 assembly commit staged only `business/`), and evaporated. #6's claim was inaccurate.
- Nothing else shipped and nothing regressed — zero code commits, per §1.

### 3. Headwinds / blockers

1. **Blocking, 7th report — no restore-tested backup of `pokewatch.db`.** Needs a human droplet-side drill. Caps the architecture composite at `min(D,R,S)+2 = 5`.
2. **Blocking, 6th report — report-email delivery unconfirmed since 2026-09-14.** Cleared only by a human checking the inbox or `journalctl -u pokewatch | grep '\[report\]'`.
3. **Re-filed with corrected history — cron-literal mismatch, claimed-fixed-but-wasn't.** `server.js:1918` says `0 13`, `.claude/skills/cto/SKILL.md:17` says `30 12`. Not re-applied this window: the file is outside `business/`, this run writes only to the section file, and repeating the same working-tree edit in an identical ephemeral sandbox would reproduce the identical evaporation. Needs a commit on `reports` that includes `.claude/`, or a human edit.
4. **Open, day 6 — nav order vs landing tab.** `src/components/TabNav.jsx:17` puts Shop first for signed-in users; `src/App.jsx:50`/`:29` still land `/` on Portfolio. Zero code commits since, so nothing has decided it. Owners: `cpo`, design (absent this cycle).
5. **Looming, unchanged:** Stripe MCP still unauthorized (this session's own startup banner). Second-brand hostname still unpointed (DNS + Caddyfile, neither in the repo).

### 4. Learnings

- **An uncommitted edit is not a delivered change.** #6 filed the cron-literal fix under Highlights; it read as delivered but the assembly commit stages only `business/`, so it evaporated. Behavioural change: a file outside `business/` is reported as changed only once `git log -- <path>` shows a commit containing it — "edited in the working tree" goes in Blockers, never Highlights.
- `CONTESTED: nav order vs landing tab still disagree (§3.4), day 6. Position unchanged: decide the default tab deliberately. Re-filed because nothing in this window addressed it.`
- `CONTESTED: whether a contributor section may report an edit outside its own directory as applied. My position: no — the assembly commit stages only business/, and #6's Highlight is the proof it doesn't survive. Either the assembly commit widens to carry .claude/ edits from contributors, or contributors are fenced to reporting and a human applies. Who may disagree: the assembler, on the grounds that widening the commit lets concurrent agents land code-adjacent files with no review.`

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-18" -- .claude/` empty.
**Applied by me:** none. The `.claude/skills/cto/SKILL.md:17` edit report #6 claimed is not present and was not re-applied (§3.3).
**Proposed:** (1) `daily-report` skill should state a contributor may report an out-of-`business/` edit as *applied* only once a commit containing it exists, otherwise it's a blocker. (2) carried, unactioned — a mechanism cited by a commit message should be opened before the claim resting on it is repeated.

**Architecture impact: none.** Zero code commits; the only write is one markdown file under `business/`. Manual steps no diff will show, unchanged: backup-and-restore drill; `REPORT_EMAIL_TO`/`RESEND_API_KEY` on the droplet; second-brand DNS/Caddyfile; Stripe MCP authorization.

---

## Product — 2026-09-21

### 1. Progress since the last report

- Zero app commits since last report — `git log --oneline --since="2026-09-18"` → only `a3cc490` (report #6's own filing commit); no code/content commits. `git status --short` empty.
- `business/product/roadmap.md` — still absent — `ls business/product/` → `daily`, `goals.md` only. Now day 6 (first noted 09-16).
- `business/product/specs/` — still absent — `ls business/product/specs/` → "No such file or directory".
- Order/product/user/watchlist counts — `not available in this environment` (no `pokewatch.db` access, droplet-only).

### 2. Highlights

- None this window — zero commits since 09-18, verified above.

### 3. Headwinds / blockers

- `business/product/roadmap.md` and `specs/` remain absent, now day 6 — blocked on a human greenlighting the roadmap/spec machinery.
- No cross-product surface changed this window, so no new instance of the nav-ordering process question — `git log --oneline --since="2026-09-18"` empty; `9c1e53e` (Sep 15) remains the only such change.

### 4. Learnings

- Standing down the nav-ordering `CONTESTED:` flag this cycle rather than re-filing a fourth time with zero new evidence. Position stands as the working default — `9c1e53e` was acceptable as a one-off; the next cross-product nav/tab change should route through `cto` via a spec — and will be re-raised if a change like it ships without one.

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-18" -- .claude/` empty.
**Proposed:** none this cycle.

---

## Marketing — 2026-09-21

### 1. Progress since the last report

- No change. `git log --oneline --since="2026-09-18"` → one commit, `a3cc490` (report #6's own filing commit) — `git show --stat` confirms it touched only `business/daily/`, `business/engineering/daily/`, `business/marketing/daily/`, `business/product/daily/`, not new marketing work.
- `git log --oneline --since="2026-09-18" -- .claude/` empty. `git status --short` empty.
- `brand.md` still does not exist — `find business -iname brand.md` empty; first noted absent 2026-09-15, now day 6.
- Order/revenue/signup counts — not available in this environment (no `pokewatch.db` access).

### 2. Highlights

- None this window — no copy, positioning, or nav change to verify.

### 3. Headwinds / blockers

- **`brand.md` still absent** — now day 6 (first noted 2026-09-15). No voice-of-record file to draft campaign copy against. Blocked on a human to create/authorize it.
- **No campaign assigned this window** — now day 5 (first noted 2026-09-16). Blocked on a human decision.
- Pricing/card-fact copy still routes through `cfo`/`catalogue-sync` respectively — moot again, nothing drafted.

### 4. Learnings

- None new this window — no commits or new artifacts (`git log --oneline --since="2026-09-18"` returns only report #6's own filing commit).

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-18" -- .claude/` empty.
**Proposed:** none new this window.

---

## Hotly debated topics

Written by the assembler from flagged `CONTESTED:` items and contradictions
detected across sections; contributors did not author this section.

### 1. Does Shop-first nav need to be reconciled with the Portfolio landing tab? (day 6, still open)

**Raised by:** `cto` (CONTESTED, engineering §4, re-filed for the third
report running). **`cpo` no longer re-files this** (product §4) — not because
the underlying position changed (both still agree `9c1e53e` should have
routed through a spec, and the next one should), but because `cpo` judged that
flagging it `CONTESTED:` again with zero new evidence is padding rather than
signal.

- **What would settle it:** `cpo` and design (absent this checkout) deciding
  Shop-first nav + Portfolio-first landing is intended, or picking one. Still
  unanswered after six days; the only thing that changed this window is
  whether it's labeled a live debate.

### 2. May a contributor report an edit outside `business/` as "applied"? (new this cycle)

**Raised by:** `cto` (CONTESTED, engineering §4), with direct evidence: report
#6 filed a `.claude/skills/cto/SKILL.md` edit as an applied Highlight, and
today's engineering §2 shows it never survived because the assembly commit
stages only `business/`. **No counter-position filed** by another domain this
cycle.

- **What would settle it:** either the assembly commit widens to carry
  contributor edits under `.claude/` when explicitly called out, or the
  `daily-report` skill states plainly that an out-of-`business/` edit is
  reported as a blocker (needing a real commit) and never as a delivered
  Highlight. `cto` has already proposed the second option in engineering §6.

---

## Blocked on a human

- **No restore-tested backup of `pokewatch.db`** — 7th report running it as a blocker; caps the architecture composite at `min(D,R,S)+2 = 5`. (engineering)
- **Report-email delivery unconfirmed** since 2026-09-14 — needs a human to check an inbox or `journalctl -u pokewatch`. (engineering)
- **Cron-literal mismatch, claimed fixed in report #6, is not fixed** — `server.js:1918` vs `.claude/skills/cto/SKILL.md:17` still disagree. Needs a commit on `reports` touching `.claude/`, or a direct human edit. (engineering)
- **Stripe MCP still unauthorized** — needs an interactive session. (engineering)
- **Second-brand hostname** — DNS record and Caddyfile edit, neither in the repo. (engineering)
- **Nav order vs Portfolio landing tab** — unresolved, day 6; see Hotly debated §1. (engineering/product)
- **Whether an out-of-`business/` contributor edit may be reported as applied** — process question for the report itself; see Hotly debated §2. (engineering)
- **`business/product/roadmap.md` and `specs/` still not stood up** — no human has greenlit the process yet, now day 6. (product)
- **`brand.md` still absent** — now day 6; no voice-of-record file exists. (marketing)
- **No marketing campaign assigned this window** — now day 5. (marketing)

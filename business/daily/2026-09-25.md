# Company daily report — 2026-09-25

**Report #2.** Anchor: `business/daily/2026-09-12.md` (report #1). Window intended
as `--since="2026-09-12"`, but see the correction below — the actual window is
`a296c01..HEAD` (the anchor's own commit boundary).

**Contributing:** engineering (`cto`), product (`cpo`), marketing (`cmo`).
Design is not in this run: `.claude/agents/head-of-design.md` does not exist in
this checkout, so the design org has not reached `master` yet. `cfo` and
`vp-support` remain out — no order and no customer message have triggered them.

**Window correction (found by `cto`, verified below):** a bare-date
`git log --since="2026-09-12"` silently drops `a296c01` (committed 2026-09-12
01:21 -0500 / 06:21 UTC — same day, but after the anchor report was written).
That commit is the one that shipped the entire executive-agent tier and the
daily-report skill itself, and it is what resolved the anchor report's
"agent/business layer uncommitted" blocker. `git log a296c01..HEAD` gives the
complete, gap-free window: one commit, `9c1e53e`. All three sections below were
verified against that corrected range, not the flawed date string.

**Sandbox limits this run:** no access to `pokewatch.db` (droplet-only,
gitignored) — every metric that would need it is marked "not available in this
environment" rather than estimated. The app could not be booted, so no
screenshots or manual verification of `9c1e53e`.

---

## Engineering — 2026-09-25

### 1. Progress since the last report

- One commit in 13 days: `9c1e53e` (2026-09-15) "Move Shop to the left of the nav bar".
  `git show --stat 9c1e53e` → `src/components/TabNav.jsx | 2 +-`, 1 file. The diff reorders
  `[...PRIVATE_TABS, SHOP_TAB]` to `[SHOP_TAB, ...PRIVATE_TABS]` for signed-in users only.
  No server, schema, dependency or deploy change. Architecture impact: none.
- Carried blocker, agent/business layer uncommitted: **resolved**. `a296c01` "Add an executive
  agent tier and a daily company report" (2026-09-12 01:21 -0500) landed after the last report
  was written. `git ls-files .claude/agents` now lists the agent files.
- Carried blocker, 48 MB untracked audio in `src/music/`: **resolved.** `test -d src/music` →
  absent. `git log --all -- src/music` → empty (never committed; git cannot show where it went).
- User and order metrics: not available in this environment (`pokewatch.db` lives only on the droplet).

### 2. Highlights

- None. The one change is a one-line nav reorder (`9c1e53e`, above).

### 3. Headwinds / blockers

- `9c1e53e` has no test coverage (`grep -rln "TabNav\|Shop" tests/` → no match), and this sandbox
  cannot boot the app to verify it. The risk is low, but the CI gate says nothing about it.
- The report brief says `business/tenets.md` and all `goals.md` files were deleted on 2026-09-15.
  At HEAD they are not: `git ls-files business/tenets.md business/engineering/goals.md` → both
  tracked, last touched by `a296c01`, and no deletion commit exists. I did not read them, as
  instructed. A human should check whether the deletion was meant to be committed.
- Unchanged: no restore-tested DB backup has been confirmed, and nothing in the repo since
  `a296c01` shows one (`git log a296c01..HEAD` → only `9c1e53e`).

### 4. Learnings

- A date-only `--since` does not reliably mark a window. `git log --since="2026-09-12"` left out
  `a296c01`, which was committed on 2026-09-12 (06:21 UTC). A SHA range has no such gap:
  `git log a296c01..HEAD` → `9c1e53e`. Future reports should anchor on the last report's HEAD SHA.

### 6. Skill and agent updates

- No change. `git log --oneline --since="2026-09-12" -- .claude/` → empty, and `a296c01..HEAD`
  touches nothing under `.claude/`.

---

## Product — 2026-09-25

### 1. Progress since the last report
- One commit in the window `--since="2026-09-12"`: `9c1e53e` "Move Shop to the left of the nav bar" (2026-09-15), sole file `src/components/TabNav.jsx`, 1 insertion/1 deletion (`git show --stat 9c1e53e`). Diff: `const tabs = user ? [...PRIVATE_TABS, SHOP_TAB] : [SHOP_TAB]` → `[SHOP_TAB, ...PRIVATE_TABS]` — for signed-in users, Shop now renders first in the tab order, ahead of Portfolio/Watchlist/etc.
- Landing behavior unchanged: commit message and `App.jsx`'s `setActiveTab(user ? 'portfolio' : 'store')` (stated in commit body, not independently re-diffed here) still send a signed-in user to Portfolio on `/`; only the nav bar's visual order shifted.
- No other commits touched `src/`, `server.js`, or `.claude/` in the window (`git log --oneline --since="2026-09-12"` returns only this one SHA; `.claude/` log is empty).

### 2. Highlights
- The one shipped change is a cross-brand-visibility tweak, not a feature: it raises Shop's prominence for existing collector users without touching the default landing tab, so it's a low-blast-radius nudge toward the storefront rather than a redirect of the funnel — evidenced by the diff itself (`src/components/TabNav.jsx`, `git show 9c1e53e`).

### 3. Headwinds / blockers
- No blockers raised in this window; `git status --short` is clean (no in-flight/uncommitted product work to unblock).

### 4. Learnings
- Nothing new to report — unchanged since 2026-09-12: no SQLite access in this sandbox (`pokewatch.db` is gitignored and droplet-only), so order counts, product counts, and any watchlist/portfolio activity remain "not available in this environment," not estimated.

### 6. Skill and agent updates
- Unchanged — checked `.claude/` commit history for the window (`git log --oneline --since="2026-09-12" -- .claude/` is empty); no skill or agent files touched.

---

## Marketing — 2026-09-25

### 1. Progress since the last report
- First-ever marketing report: `business/marketing/daily/` did not exist before this write (`ls business/marketing/daily` → "No such file or directory"). Nothing to diff against.
- Repo-wide window `--since="2026-09-12"`: one commit total, `9c1e53e` (`git log --oneline --since="2026-09-12"`). No commit touched `business/` in the window (`git log --oneline --since="2026-09-12" -- business/` → empty).
- `business/marketing/` contains only `goals.md` — `find business/marketing -type f` → `business/marketing/goals.md`, no `content/` dir, no drafted copy anywhere in the domain.

### 2. Highlights
- `9c1e53e` (`src/components/TabNav.jsx`, +1/-1, 2026-09-15): Shop moved ahead of Portfolio in the signed-in nav order. Verified independently rather than taking the handed-down summary at face value: `src/App.jsx:47-51` (`tabFromPath`) sends `/` to `'portfolio'` regardless of tab-list order, and `src/App.jsx:235` forces the logo click to `user ? 'portfolio' : 'store'` too — so the landing experience is unchanged; only left-to-right nav order shifted. No user-visible copy changed, nothing to draft around.
- Store still has nothing to promote: 2026-09-12 anchor reported `products=0`, `orders=0`; this sandbox has no `pokewatch.db` access to re-check, consistent with a genuinely quiet window.

### 3. Headwinds / blockers
- `business/marketing/brand.md` — the voice-of-record this role is told to keep, not re-derive — does not exist (`find business -iname "*brand*"` → no results). Any copy drafted from here has no source of truth to check tone against until a human (re)creates it.
- No DB access in this environment: product/price/stock facts can't be verified beyond the 2026-09-12 baseline, so storefront campaign copy stays blocked per the verify-before-write rule.

### 4. Learnings
- The one commit this window is an engineering-surface nav reorder, not a content asset — confirmed via App.jsx rather than assumed, useful precedent for future report-by-exception passes on nav diffs. No disagreements to flag.

### 6. Skill and agent updates
- No commits under `.claude/` this window (`git log --oneline --since="2026-09-12" -- .claude/` → empty; confirmed by the repo-wide log showing only `9c1e53e`). Nothing scanned or adopted this cycle.

---

## Hotly debated topics (assembled)

Written by the assembler from flagged `CONTESTED:` items and contradictions
between sections. Contributors do not author this section.

None this period. No contributor raised a `CONTESTED:` flag, and reading across
all three sections finds no factual or recommendation-level contradiction — all
three independently confirm the same single commit (`9c1e53e`) and its narrow,
nav-order-only scope, and none of their headwinds conflict with another's.

---

## Blocked on a human

- **`business/tenets.md` and `goals.md` files still exist, contrary to this
  run's brief.** The brief stated they were deleted 2026-09-15 and being
  rebuilt; at HEAD, `git ls-files business/tenets.md business/engineering/goals.md`
  shows both tracked, last touched by `a296c01` (2026-09-12), no deletion commit
  exists. Per `cto`, §3. A human should confirm whether a deletion was intended
  but never committed, or the brief is simply stale — this run did not read
  either file, per instruction, so their current content is unverified.
- **No restore-tested backup of the production SQLite file.** Carried unchanged
  from the anchor report; still no evidence of one in the repo or on any
  documented droplet step (`cto`, §3).
- **`business/marketing/brand.md` does not exist.** The marketing voice-of-record
  this role is meant to consult has never been written (`cmo`, §3) — blocks any
  future storefront copy from being checked against a standard rather than
  invented per-piece.

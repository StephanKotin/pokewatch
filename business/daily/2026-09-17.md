# Company daily report — 2026-09-17

**Report #5.** Anchor: `business/daily/2026-09-16.md` (1 day). Git evidence
window: `--since="2026-09-16"` on `origin/master`.

**Contributing:** engineering, product, marketing. `.claude/agents/head-of-design.md`
is absent from this checkout, so design does not file this cycle — three
contributors, not four. `cfo`/`vp-support` join only on their triggers (first
real order / first customer message); neither has fired.

**Goals/tenets:** `business/tenets.md` and every `goals.md` were removed
2026-09-15 and are being rebuilt. No goals-and-tenet-coverage section this
report; none is cited by name below.

**Environment note:** cloud sandbox. No access to `pokewatch.db` (droplet-only,
gitignored) — DB-derived metrics are marked `not available in this
environment` throughout, never estimated. The app cannot be booted or
screenshotted from here.

**Shared facts, verified once and handed to each contributor** (each also
re-ran its own domain-scoped queries): `git log --oneline --since="2026-09-16"
origin/master` → empty; same query scoped `-- .claude/` → empty; `git status
--short` → empty. **Zero commits, zero uncommitted work, since the last
report.** This is a quiet day by construction, not by omission — every
section below reports by exception against that fact.

---

## Engineering — 2026-09-17

**Report #5.** Owner `cto`. Anchor `business/daily/2026-09-16.md`. Window: since 2026-09-16. Re-verified rather than trusted: `git log --oneline --since="2026-09-16" origin/master` → empty; same query scoped `-- .claude/` → empty; `git status --short` → empty; `git log --oneline -3 origin/master` → head still `9c1e53e`, the commit already reported on 09-16. **No code changed in this window.** Reporting by exception below.

### 1. Progress since the last report

- Commits on `origin/master` — was **1**, now **0** — query above. `9c1e53e` is unchanged as head and was covered in the 09-16 report; not re-reported here.
- Specs — **no change since the last report, still 7** — `grep -c "test(" tests/*.spec.js` → `orders.spec.js:5`, `smoke.spec.js:2`.
- Restore-tested backup of `pokewatch.db` — **no change: still none** — `grep -rniE "backup|litestream|restore" .github/workflows/ server.js package.json` → one hit, the same unrelated comment at `server.js:1064`.
- Report payload size — was **15,646 B**, now **13,998 B** (−11%) — `git show HEAD:business/daily/2026-09-16.md | wc -c`. Second consecutive fall; `business/daily/2026-09-16-digest.md` exists on this branch (`git ls-tree --name-only HEAD business/daily/`).
- Row counts, email-delivery counts, droplet state — **not available in this environment**; `pokewatch.db` is gitignored and droplet-only. Not estimated.

### 2. Highlights

- **Nothing shipped, and nothing regressed.** The one genuinely new observation is the payload trend in §1: the report format has now shrunk two windows running without losing citations, which retires the Gmail-clipping risk withdrawn on 09-16 rather than merely deferring it.
- No verifier or release-guard work was warranted: with an empty diff there is nothing to prove in the running app.

### 3. Headwinds / blockers

1. **Blocking, 5th report running — no restore-tested backup.** Re-checked today, not assumed; query in §1. Still blocked on a human droplet-side drill. Still caps the architecture composite at `min(D,R,S)+2 = 5`.
2. **Blocking, 4th report — report email delivery still unconfirmed.** Re-checked: every failure path in `emailDailyReport` still logs and returns silently (`grep -n "cron.schedule" server.js` → `server.js:1918`). Cleared only by a human checking an inbox or `journalctl -u pokewatch | grep '\[report\]'`.
3. **Blocking, unfixed since 09-14 (4 days) — cron literal mismatch.** Confirmed still wrong today: `.claude/skills/cto/SKILL.md:17` says `30 12 * * 1-5`; `server.js:1918` says `cron.schedule('0 13 * * 1-5', emailDailyReport, { timezone: 'UTC' })`. 30 minutes off, in the direction that misdirects a debug of blocker 2.
4. **Open, unresolved by anything in this window — nav order vs landing tab.** `src/components/TabNav.jsx:17` → `user ? [SHOP_TAB, ...PRIVATE_TABS]`; `src/App.jsx:50` still returns `'portfolio'` for `/`. Zero commits since, so nothing has decided it. Owners: `cpo`, `vp-design`.
5. **Looming, unchanged:** Stripe MCP unauthorized in this session; second-brand hostname still unpointed.

### 4. Learnings

- **A carried-forward blocker has to be re-queried, not re-typed.** All four of today's carry-forwards were re-run against the working tree before being restated; the cron mismatch at `SKILL.md:17` in particular would read identically whether it had been fixed or not. **Behavioural change:** a blocker restated without a fresh query is a claim about yesterday, not today, and does not get written.
- **An empty window is cheap to report and expensive to pad.** Today's section cost five greps; the temptation to re-derive `9c1e53e`'s `--stat` for a second consecutive report would have produced volume and zero new information.
- `CONTESTED: nav order and landing tab still disagree (§3.4) — signed-in users see Shop first but opening / lands on Portfolio. My position is unchanged: decide the default tab deliberately rather than let a one-line nav tweak imply a change it did not make. Re-filed because nothing in this window addressed it, not because it is newly contested.`

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-16" origin/master -- .claude/` → empty.
**Proposed:** (1) **re-filed for the 3rd time, unchanged** — delete the `30 12 * * 1-5` literal at `.claude/skills/cto/SKILL.md:17` and keep the `grep -n "cron.schedule" server.js` recipe already beside it (blocker 3). It is a one-word edit that has now outlived three reports, which is itself the argument for making it. (2) **carried, unactioned** — the `daily-report` §2 rule that a mechanism cited by a commit message is opened before the claim resting on it is repeated. (3) No new agent or worker warranted.

**Architecture impact: none.** Zero commits in the window — no route, table, column, dependency, env var, deploy step or auth path changed. **Manual steps no diff will show, unchanged:** the backup-and-restore drill; `REPORT_EMAIL_TO` and `RESEND_API_KEY` in `.env` on the droplet; the second brand's DNS record and Caddyfile edit; the Stripe MCP authorization.

---

## Product — 2026-09-17

### 1. Progress since the last report

- Zero commits since the last report — `git log --oneline
  --since="2026-09-16" origin/master` returns empty.
- `git status --short` — empty; no uncommitted work in progress.
- `business/product/roadmap.md` — still absent — `ls business/product/`
  (contents: `daily`, `goals.md`).
- `business/product/specs/` — still absent — `ls business/product/specs/`
  errors "No such file or directory".
- Order/product/user/watchlist counts — `not available in this environment`
  (no access to `pokewatch.db`; droplet-only).

### 2. Highlights

- None this window — no new commits to highlight (`git log --oneline
  --since="2026-09-16" origin/master`, empty).

### 3. Headwinds / blockers

- `business/product/roadmap.md` and `specs/` remain absent, unchanged for a
  second day — blocked on a human greenlighting the roadmap/spec machinery
  (age: 2 days, first noted `business/product/daily/2026-09-16.md`).
- The no-spec-preceded-`9c1e53e` process gap noted yesterday has no new
  instance to point to today — nothing shipped this window to test it against
  (`git log --oneline --since="2026-09-16" origin/master`, empty).

### 4. Learnings

- The CONTESTED item from yesterday (whether `9c1e53e`'s nav-order change
  should have routed through `cto`) has not been addressed — no commits, and
  no counter-position filed, this window.

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-16" origin/master
-- .claude/` is empty.

**Proposed:** none this cycle.

---

## Marketing — 2026-09-17

### 1. Progress since the last report
- No change. Zero commits since the last report — `git log --oneline --since="2026-09-16" origin/master` returns empty, confirmed independently this window.
- `git status --short` on `reports` branch (HEAD `52dd750`) is empty — no uncommitted marketing work either.
- `brand.md` still does not exist — `find business -iname brand.md` returns nothing; unchanged since `business/marketing/daily/2026-09-16.md`, now day 3 of this absence.
- Order/revenue/signup counts — not available in this environment (no access to `pokewatch.db`, gitignored, droplet-only); unchanged caveat.

### 2. Highlights
- None this window — no commits landed (`git log --oneline --since="2026-09-16" origin/master` empty), so no copy, positioning, or nav change to verify or report on.

### 3. Headwinds / blockers
- **`brand.md` still absent** — carried forward, now 3 days open (first noted 2026-09-15). No voice-of-record file to draft campaign copy against.
- **No campaign assigned this window** — carried forward, 1 day. Blocked on a human decision, not actionable by me.
- Pricing/card-fact copy still routes through `cfo` or `catalogue-sync` ground rules respectively — moot again, no asset drafted this window.

### 4. Learnings
- None new this window — no commits or new artifacts to draw a learning from (`git log --oneline --since="2026-09-16" origin/master` empty).

### 6. Skill and agent updates
**Automated:** none — `git log --oneline --since="2026-09-16" origin/master -- .claude/` is empty.
**Proposed:** none new this window.

---

## Hotly debated topics

Written by the assembler from flagged `CONTESTED:` lines and contradictions
detected across sections; contributors did not author this section. Both
items below are unchanged from `business/daily/2026-09-16.md` — zero commits
this window means nothing moved either one.

### 1. Does Shop-first nav need to be reconciled with the Portfolio landing tab? (day 2, still open)

**Raised by:** `cto` (CONTESTED, engineering §4, re-filed — not newly
contested). Position unchanged: `src/components/TabNav.jsx:17` puts Shop
first for signed-in users, but `src/App.jsx:50` still lands `/` on Portfolio
regardless of `user`; pick one deliberately.

- **No counter-position filed.** Design still has no voice in this checkout
  (`head-of-design.md` absent). `cpo`'s section today does not take the
  design side either — it only confirms nothing new happened.
- **What would settle it:** `cpo` and `vp-design` (once that role exists
  here) state whether Shop-first nav + Portfolio-first landing is intended,
  or one should change to match. Still unanswered.

### 2. Should a cross-product nav-ordering change route through a spec before shipping? (day 2, still open)

**Raised by:** `cpo` (CONTESTED on 2026-09-16, product §4/§4 today).
Position unchanged: `9c1e53e` decided this without a spec; acceptable once
given the tiny diff, but the next such change should route through `cto`
first.

- **No counter-position filed.** `cto`'s section does not take a process
  position either day.
- **What would settle it:** whether `cto` agrees the process gap is worth
  closing. No new instance arrived this window to force the question.

---

## Blocked on a human

- **No restore-tested backup of `pokewatch.db`** — 5th report running it as a
  blocker; caps the architecture composite at `min(D,R,S)+2 = 5`. (engineering)
- **Report-email delivery unconfirmed** since 2026-09-14 — needs a human to
  check an inbox or `journalctl -u pokewatch`. (engineering)
- **Stripe MCP still unauthorized** — needs an interactive session. (engineering)
- **Second-brand hostname** — DNS record and Caddyfile edit, neither in the
  repo. (engineering)
- **`business/product/roadmap.md` and `specs/` still not stood up** — no human
  has greenlit the process yet, now 2 days. (product)
- **No marketing campaign assigned this window.** (marketing)

Not included above: the `.claude/skills/cto/SKILL.md:17` cron-literal mismatch
(engineering §3.3) is a one-line fix `cto` can make directly and has re-filed
three reports running without applying — that is a self-clearable backlog
item, not a human blocker.

# Company daily report — 2026-09-24

**Report #2.** Anchor: `business/daily/2026-09-12.md` (commit `a296c01`, the
commit that filed it). Window: `a296c01..HEAD`, which resolves to a single
commit — `9c1e53e Move Shop to the left of the nav bar` (2026-09-15). No
commits touched `.claude/` in the window; `git status --short` is clean.

**Contributing:** engineering (`cto`), product (`cpo`), marketing (`cmo`).
Design did not file this run: `.claude/agents/head-of-design.md` does not
exist in this checkout, so the design org has not reached `master` yet.
`vp-research` no longer exists on this roster. `cfo` and `vp-support` are out
by design until there is a real order or a support message.

Goals and tenet coverage is omitted this run: `business/tenets.md` and every
`goals.md` were reported deleted and being rebuilt, so no section grades
against them and none is cited by name below. Note: engineering found those
files still present on disk (see Blocked on a human) — flagged, not resolved.

This is a cloud-sandbox run: no access to `pokewatch.db` (droplet-only,
gitignored), and the app was not booted or screenshotted.

---

## Engineering — 2026-09-24

### 1. Progress since the last report

- Commits in window — was 9, now 1 — `git log --since="2026-09-12"` → `9c1e53e` only
- Uncommitted work — was 2 modified + 17 untracked, now 0 — `git status --short` empty; the agent layer landed in `a296c01` (2026-09-12, the commit that carried the last report)
- Untracked audio in `src/music/` — was 48 MB, now absent — `ls -d src/music` → no such file
- Test count — unchanged at 7 — `grep -c "test(" tests/*.spec.js` → `orders:5`, `smoke:2`
- CI deploy gate — unchanged, intact — `.github/workflows/deploy.yml:21` `needs: test`
- Restore-tested backup — unchanged, none — `grep -rniE "litestream|\.backup\(" .github/workflows/ server.js package.json` → no hits
- Users, orders, products row counts — not available in this environment (no `pokewatch.db`)

### 2. Highlights

- `9c1e53e`: one line changed in `src/components/TabNav.jsx`. Signed-in users now see Shop first in the nav. It is the only change in the window. Putting Shop first was product's decision, so I am not counting it as an engineering highlight. My only engineering claim: per the commit message, the default landing tab is unchanged. Architecture impact: none.

### 3. Headwinds / blockers

- Still no restore-tested backup of the droplet's SQLite file (see grep above). This caps the architecture composite at 5, and nothing built in this window can lift it. Blocked on a human: someone has to choose a backup target and run one restore on the droplet. No diff will show that step.
- The brief says tenets and goals were deleted on 2026-09-15, but this checkout still has `business/tenets.md` and all six `goals.md` files. `git status` is clean, and no commit after `a296c01` touches them. So the deletion did not happen in this repo, or it has not been pushed yet. I have not read them. A human should confirm where the rebuild is happening.
- The Stripe MCP server did not connect this session: `ERR_PROXY_TUNNEL: 403 Forbidden`, from the startup notice on 2026-09-24. Live Stripe state still cannot be read from a sandbox session.

### 4. Learnings

- `git log --since="2026-09-12"` leaves out `a296c01`, even though that commit is dated the anchor day. When git gets a date with no time, it fills in the current clock time, not midnight. A commit that lands on the anchor date can therefore fall between two reports. Behavioural change: I now anchor the window on the last report's commit SHA (`a296c01..HEAD`) instead of a date.

---

## Product — 2026-09-24

### 1. Progress since the last report
- Commits since 2026-09-12 — was 0 known, now 1 (`9c1e53e`) — `git log --oneline --since="2026-09-12"`.
- Order/product/user/watchlist metrics — not available in this environment (no access to `pokewatch.db`, which is gitignored and lives only on the droplet).
- Everything else (specs drafted, roadmap sequence) — unchanged since 2026-09-12 — `git log --oneline --since="2026-09-12" -- .claude/` and `git log --oneline --since="2026-09-12" -- business/product/` both empty.

### 2. Highlights
- `9c1e53e` "Move Shop to the left of the nav bar" — `src/components/TabNav.jsx:17`, one line changed (`git show --stat 9c1e53e`). For signed-in users, tab order becomes `[Shop, ...PRIVATE_TABS]` instead of `[...PRIVATE_TABS, Shop]`; signed-out visitors see only Shop either way, so unaffected. This is a nav-order tweak, not a feature — no route, table, or column touched, so no scorecard entry is warranted. Worth flagging only because it nudges signed-in collector users toward the storefront tab first; the default landing tab is unchanged (`App.jsx`'s `setActiveTab(user ? 'portfolio' : 'store')`, per the commit body), so this doesn't override the product's existing collector-first default — it's additive emphasis, not a reversal.

### 3. Headwinds / blockers
- No spec or roadmap entry backs the nav-order change above — it shipped as a direct code commit outside the spec → cto → backend/frontend path. Not blocking anything today, but worth a product decision on whether "Shop first for signed-in users" is an intentional storefront-promotion move or a one-off; nobody has stated it as policy.
- No other blockers this period — `git status --short` clean, single commit in window.

### 4. Learnings
- None this period beyond the above: the codebase saw exactly one commit in twelve days, confirming the storefront and collector are both in a quiet/pre-traffic state rather than actively iterating.

---

## Marketing — 2026-09-24

### 1. Progress since the last report
- Commits touching marketing-relevant surfaces since 2026-09-12 — was 0, now 1 — `git log --oneline --since="2026-09-12"` shows only `9c1e53e Move Shop to the left of the nav bar` (2026-09-15).
- Storefront traffic / conversion / order counts — not available in this environment (requires `pokewatch.db`, which is gitignored and lives only on the droplet; this sandbox has no copy).
- `business/marketing/` content files (campaign briefs, drafts) — unchanged: directory holds only `goals.md`, no campaign or draft assets added or removed this window (`ls business/marketing/`).
- `.claude/` skill/agent changes relevant to marketing work — unchanged — `git log --oneline --since="2026-09-12" -- .claude/` returns empty.

### 2. Highlights
- `9c1e53e` (`src/components/TabNav.jsx`, 1 line changed) reorders the signed-in nav so Shop sits ahead of Portfolio, matching what signed-out visitors already saw. Verified via `git show --stat 9c1e53e`. This is a real storefront-visibility change worth noting to the team, but it's a `cpo`/engineering-routed UI decision, not a marketing deliverable — no copy, claim, or campaign asset attaches to it. The commit message itself confirms the default landing tab is unchanged (`App.jsx`'s `setActiveTab(user ? 'portfolio' : 'store')`), so no "new landing experience" claim is supportable.

### 3. Headwinds / blockers
- No campaign work, competitor research requests, or copy drafts were logged in `business/marketing/` this window — `ls business/marketing/` shows only `goals.md`, nothing dated between 2026-09-12 and today. Not a blocker on a human, just an empty window: one commit total in the whole repo (`git log --oneline --since="2026-09-12"`) gave nothing new to promote or verify claims against.
- Any metric that would ordinarily anchor a campaign brief (traffic, order volume, conversion) is not available in this environment — flagged above, not fabricated.

### 4. Learnings
- None this period — no new tooling, workflow, or capability was evaluated or adopted in this window (no commits under `.claude/` per `git log --oneline --since="2026-09-12" -- .claude/`, and no marketing-authored files changed other than what's noted above).

---

## Hotly debated topics

None this period. No `CONTESTED:` flag was raised by any contributor. Engineering,
product and marketing all read the window's one commit (`9c1e53e`) the same
way — a small nav-order change, not a feature, outside any single domain's
deliverables — so there is no contradiction to record.

---

## Blocked on a human

- **No restore-tested backup of `pokewatch.db`.** Caps the architecture
  composite at 5 regardless of other work. Someone needs to pick a backup
  target and run one restore on the droplet — engineering §3.
- **Tenets/goals deletion status is unresolved.** This run's brief says
  `business/tenets.md` and every `goals.md` were deleted on 2026-09-15; this
  checkout still has all of them, unmodified since `a296c01`. Needs a human to
  say where the rebuild is actually happening (a different branch/checkout?)
  before future reports can trust either claim — engineering §3.
- **Stripe MCP server did not connect this session** (`ERR_PROXY_TUNNEL: 403`).
  Needs an interactive session to reauthorize; blocks any Stripe-sourced
  reporting (`cfo` isn't on the roster yet regardless) — engineering §3.
- **Nav-order policy undecided.** `9c1e53e` put Shop ahead of Portfolio for
  signed-in users with no spec behind it. Not blocking, but nobody has said
  whether "Shop first" is intentional storefront-promotion policy or a
  one-off — product §3.

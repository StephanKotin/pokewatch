# Company daily report — 2026-09-22

**Report #8.** Anchor: `business/daily/2026-09-21.md` (report #7, filed on the
`reports` branch). Window for all git evidence: `--since="2026-09-21"`,
checked independently on both `master` (HEAD detached at `9c1e53e`,
2026-09-15) and `origin/reports` — both empty of app-code commits; the only
commits on `reports` since the anchor are report #7's own filing commits
(`d7fa041`, `a60cfc8`, `e3c08be`). No branch disagreement this cycle.

**Contributing:** engineering, product, marketing. `.claude/agents/head-of-design.md`
is absent from this checkout (`ls` → no such file), so design does not file
this cycle — three contributors, not four. `cfo`/`vp-support` join only on
their triggers (first real order / first customer message); neither has
fired. `vp-research` is not on the roster.

**Goals/tenets:** not read, not cited, not recreated this cycle. `business/tenets.md`
and every `goals.md` are being rebuilt and are out of scope for this run per
explicit instruction.

**Environment note:** cloud sandbox. No access to `pokewatch.db` (droplet-only,
gitignored) — DB-derived metrics are marked `not available in this
environment` throughout, never estimated. The app cannot be booted or
screenshotted from here.

**Zero commits anywhere since the last report — fourth consecutive quiet
window for the app itself.**

---

## Engineering — 2026-09-22

**Report #8.** Owner `cto`. Anchor `business/daily/2026-09-21.md` (report #7). Window `--since="2026-09-21"`. Shared facts confirmed, not assumed: I ran the window query on **both** branches — `master` (HEAD detached at `9c1e53e`) → empty, `origin/reports` → `d7fa041`, `a60cfc8`, `e3c08be`, all three report #7's own filing commits and none past it. `git status --short` → empty. So zero commits of any kind since the anchor, and the branch disagreement that #7 had to correct did not recur.

### 1. Progress since the last report

- Code commits — **0**, unchanged since 2026-09-18. Newest commit touching `server.js` or `src/` is still `9c1e53e` "Move Shop to the left of the nav bar" (`git log --oneline -3 -- server.js src/`). Fourth consecutive window with no code change.
- Specs — unchanged since 2026-09-21, **7** (`grep -c "test(" tests/*.spec.js` → `orders.spec.js:5`, `smoke.spec.js:2`).
- Restore-tested backup — unchanged since 2026-09-21: still none.
- Report payload size — was **13,064 B**, now **12,450 B** (−5%) — `git show origin/reports:business/daily/2026-09-21.md | wc -c`. Reverses last window's rise; three of the last four windows have fallen.
- Row counts, email-delivery counts, droplet state — **not available in this environment** (`pokewatch.db` is gitignored and droplet-only; the app cannot be booted here). Not estimated.

### 2. Highlights

- None, and that is the accurate entry. Zero commits on either branch (header), clean tree, no blocker cleared. Nothing shipped and nothing regressed.

### 3. Headwinds / blockers

1. **Blocking, 8th report running — no restore-tested backup of `pokewatch.db`.** Re-queried today: `grep -rniE "backup|litestream|restore" .github/workflows/ server.js package.json` → one hit, the unrelated comment at `server.js:1064`. Needs a human droplet-side drill. Still caps the architecture composite at `min(D,R,S)+2 = 5`.
2. **Blocking, 7th report — report-email delivery unconfirmed since 2026-09-14.** Unchanged since 2026-09-21; `grep -n "cron.schedule" server.js` → `1800` (`scanPrices`), `1918` (`emailDailyReport`), and every failure path in `emailDailyReport` still logs and returns silently. Cleared only by a human checking the inbox or `journalctl -u pokewatch | grep '\[report\]'`.
3. **Blocking, 3rd report — cron-literal mismatch, still live.** Both sides re-read today: `server.js:1918` → `cron.schedule('0 13 * * 1-5', emailDailyReport, { timezone: 'UTC' })`; `.claude/skills/cto/SKILL.md:17` → `emailDailyReport` (`30 12 * * 1-5`, pinned to UTC). A 30-minute drift in the one document an agent reads to know when the report mails. Newest commit touching that skill file is `a296c01`, which predates report #6's claim to have fixed it. New this window: `git log --oneline --since="2026-09-21" -- .claude/` is empty on **both** `master` and `origin/reports`, so there is still no landed path for a contributor edit outside `business/` — see §4. Not re-attempting the working-tree edit a third time.
4. **Open, day 7 — nav order vs landing tab.** Re-queried, both unchanged: `src/components/TabNav.jsx:17` → `const tabs = user ? [SHOP_TAB, ...PRIVATE_TABS] : [SHOP_TAB];`; `src/App.jsx:29` → `portfolio: '/'` with `:50` falling back to `'portfolio'`. **I am not re-filing this as CONTESTED this cycle.** I flagged it three reports running and have no new evidence — zero code commits means nothing has moved the facts, and `cpo` reached the same judgment last cycle. Restating a position without new evidence is padding. It stays here as an open item for `cpo`/`vp-design`; I will re-flag it if a commit touches either file or a user-behaviour number arrives.
5. **Looming, unchanged since 2026-09-21:** Stripe MCP still unauthorized — this session's own startup banner lists `stripe` under servers needing authentication, and the session is non-interactive. Second-brand hostname still unpointed (DNS record + Caddyfile edit, neither in the repo, neither protected by `reset --hard`).

### 4. Learnings

- **A quiet window is cheap to report and expensive to pad.** This window's entire engineering state was established by four commands — the window query on two branches, `git status`, and two greps. #7 spent its length correcting #6's inaccurate claim; that cost existed because #6 padded. The discipline that prevents it is the one that makes a report this short: file what a command returned, and nothing that only an intention supports.
- **A blocker I have chosen not to re-attempt needs a named owner, or it decays into scenery.** §3.3 is on its third report with the same evidence and no change in who can clear it. Stating it again is only worth the line because the §4 flag below names the decision that unblocks it; without that, item 3 should be closed as won't-fix rather than carried a fourth time.
- `CONTESTED: may a contributor report an edit outside business/ as "applied"? My position, unchanged and still uncountered after one cycle: it may not, because the assembly commit stages only business/ — §3.3 is now three reports of proof that such an edit evaporates. This needs a decision, not a fourth restatement: either the assembly commit widens to carry contributor edits under .claude/, or contributors are fenced to reporting and a human applies. Who may disagree: the assembler, on the grounds that widening the commit lets five concurrent agents land code-adjacent files with no review. If no counter-position is filed this cycle, I will take the fence as settled and stop carrying §3.3.`

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-21" -- .claude/` → empty on both `master` and `origin/reports`, re-run by me.
**Applied by me:** none. My only write this run is this file.
**Proposed:** (1) carried, second consecutive report — the `daily-report` skill should state that an edit outside `business/` counts as *applied* only once `git log -- <path>` shows a commit containing it; otherwise it is a blocker. (2) carried, unactioned — a mechanism cited by a commit message is opened before a claim is rested on it. (3) No new agent or worker warranted.

**Architecture impact: none.** Zero commits in the window; my only write is one markdown file under `business/`, touching no route, table, column, dependency, env var, deploy step or auth path. **Manual steps no diff will show, unchanged since 2026-09-21:** the backup-and-restore drill; `REPORT_EMAIL_TO` and `RESEND_API_KEY` in `.env` on the droplet; the second brand's DNS record and Caddyfile edit; the Stripe MCP authorization.

---

## Product — 2026-09-22

**Report #8.** Anchor: `business/daily/2026-09-21.md` (report #7, on
`reports`, not this `master` checkout). Window: since 2026-09-21.

### 1. Progress since the last report

- Zero commits since the anchor — `git log --oneline --since="2026-09-21"` on
  `master` (HEAD detached at `9c1e53e`) → empty; same on `origin/reports` →
  three commits, all report #7's own filing (`d7fa041`, `a60cfc8`, `e3c08be`),
  none touching app code. `git status --short` empty here.
- `business/product/roadmap.md` — still absent — `ls business/product/` →
  `goals.md` only. Now **day 7** (first noted 09-16, inclusive count).
- `business/product/specs/` — still absent — "No such file or directory".
- Order/product/user/watchlist counts — not available in this environment (no
  `pokewatch.db` access, droplet-only).

### 2. Highlights

- None this window — zero commits since 09-21, verified above.

### 3. Headwinds / blockers

- Roadmap/specs still absent, now day 7 — blocked on a human greenlight.
- No cross-product surface changed this window — `9c1e53e` (Sep 15) remains
  the only such change to date.

### 4. Learnings

- No new evidence to re-raise the nav-order-vs-landing-tab question
  (`9c1e53e`, unchanged) — not flagging `CONTESTED:` again this cycle, same
  reasoning as last: re-filing with zero new commits is padding, not signal.
  Position unchanged: the next cross-product nav/tab change should route
  through `cto` via a spec.

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-21" -- .claude/` on
`master` → empty (re-verified).
**Proposed:** none this cycle.

---

## Marketing — 2026-09-22

### 1. Progress since the last report
- No change. Checked both branches independently per this run's instructions: `git log --oneline --since="2026-09-21"` on `master` (HEAD `9c1e53e`) → empty; `git log origin/reports --oneline --since="2026-09-21"` → empty. `git log origin/reports -3 --format='%h %cI %s'` shows the newest commits are `d7fa041`/`a60cfc8`/`e3c08be`, all timestamped `2026-09-21T12:0x`Z — the report #7 filing commits themselves, not new work.
- `git log origin/reports --oneline --since="2026-09-21" -- .claude/` → empty; same on `master` → empty. No skill or agent change touching marketing.
- `git status --short` → empty on this checkout — nothing in flight.
- Order/revenue/signup counts — not available in this environment (no access to `pokewatch.db`, droplet-only, gitignored); unchanged caveat, carried from every prior report.

### 2. Highlights
- None this window. Zero commits anywhere since the anchor — no copy, positioning, or nav change to verify or report on.

### 3. Headwinds / blockers
- **`brand.md` still absent, day 8** (first noted 2026-09-15) — `find business -iname brand.md` returns nothing. No voice-of-record file exists to draft campaign copy against. Blocked on a human to create/authorize it.
- **No campaign assigned this window, day 7** (first noted 2026-09-16) — blocked on a human decision, not actionable by me.
- Pricing and card-fact copy still route through `cfo` (products/orders tables) and the `catalogue-sync` skill's ground rules respectively — moot again this window since no asset was drafted.

### 4. Learnings
- None new this window — no commits or new artifacts to draw a learning from. Note carried from `cto`'s report #7 (`origin/reports:business/engineering/daily/2026-09-21.md`): an uncommitted working-tree edit is not a delivered change — applies equally here, so nothing below the line above gets claimed as done without a commit SHA.

### 6. Skill and agent updates
**Automated:** none — `.claude/` unchanged on both `master` and `origin/reports` since the anchor, confirmed above.
**Proposed:** none new this window.

---

## Hotly debated topics

Written by the assembler from flagged `CONTESTED:` items and contradictions
detected across sections; contributors did not author this section.

### 1. May a contributor report an edit outside `business/` as "applied"? (uncountered a second cycle — resolving by default)

**Raised by:** `cto` (CONTESTED, engineering §4, second report running this
exact flag). **No counter-position filed** by `cpo` or `cmo` this cycle
either — same as last cycle.

- **`cto`'s position:** no. The assembly commit stages only `business/`, so
  an edit under `.claude/` never lands from a contributor's own write; three
  reports running (#6, #7, #8) have shown the `.claude/skills/cto/SKILL.md:17`
  cron-literal mismatch persist despite report #6 once claiming it fixed.
- **What would settle it:** `cto` set its own termination condition —
  absent a counter-position this cycle, treat the fence as settled (may not
  report as applied) and stop carrying engineering §3.3 as a distinct proof
  point going forward. No counter-position landed, so by that stated rule
  this is resolved by default as of this report; expect it dropped from
  future Hotly Debated sections unless someone reopens it. **The underlying
  documentation mismatch itself is not resolved** — it remains a live
  blocker (see Blocked on a human) until a real commit lands the fix.

None this period on the nav-order-vs-landing-tab question: both `cto`
(engineering §3.4) and `cpo` (product §4) independently declined to
re-flag it `CONTESTED:` this cycle, agreeing that restating an unchanged
position with zero new evidence is padding. It remains open (see Blocked on
a human) but is not a live debate this window.

---

## Blocked on a human

- **No restore-tested backup of `pokewatch.db`** — 8th report running it as a blocker; caps the architecture composite at `min(D,R,S)+2 = 5`. (engineering)
- **Report-email delivery unconfirmed** since 2026-09-14 — needs a human to check an inbox or `journalctl -u pokewatch`. (engineering)
- **Cron-literal mismatch** — `server.js:1918` vs `.claude/skills/cto/SKILL.md:17` still disagree by 30 minutes, 3rd report running. Needs a real commit on `reports` touching `.claude/`, or a direct human edit — no contributor write reaches that path. (engineering)
- **Stripe MCP still unauthorized** — needs an interactive session. (engineering)
- **Second-brand hostname** — DNS record and Caddyfile edit, neither in the repo. (engineering)
- **Nav order vs Portfolio landing tab** — open, day 7; not a live debate this cycle (see Hotly debated), but still undecided. (engineering/product)
- **`business/product/roadmap.md` and `specs/` still not stood up** — no human has greenlit the process yet, now day 7. (product)
- **`brand.md` still absent** — now day 8; no voice-of-record file exists. (marketing)
- **No marketing campaign assigned this window** — now day 7. (marketing)

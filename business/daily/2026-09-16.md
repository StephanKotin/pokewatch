# Company daily report — 2026-09-16

**Report #4.** Anchor: `business/daily/2026-09-15.md` (1 day). Git evidence
window: `--since="2026-09-15"` on `origin/master`.

**Contributing:** engineering, product, marketing. `.claude/agents/head-of-design.md`
is absent from this checkout, so design does not file this cycle — three
contributors, not four. `cfo`/`vp-support` join only on their triggers (first
real order / first customer message); neither has fired.

**Goals/tenets:** `business/tenets.md` and every `goals.md` were removed
2026-09-15 and are being rebuilt. No goals-and-tenet-coverage section this
report; none is cited by name below.

**Environment note:** cloud sandbox. No access to `pokewatch.db` (droplet-only,
gitignored) — DB-derived metrics are marked `not available in this
environment` throughout, never estimated. The app cannot be booted or
screenshotted from here.

---

## Engineering — 2026-09-16

**Report #4.** Owner `cto`. Anchor `business/daily/2026-09-15.md`. Window: since 2026-09-15. Re-verified rather than trusted: `git log --oneline --since="2026-09-15" origin/master` → one commit, `9c1e53e`; `git show --stat 9c1e53e` → `src/components/TabNav.jsx | 2 +-`; `git status --short` → empty; `git log --since="2026-09-15" origin/master -- .claude/` → empty. The hand-off was accurate on all four.

### 1. Progress since the last report

- Commits on `origin/master` — was **0**, now **1** — `git ls-remote --heads origin` → `master = 9c1e53eab4d0…`, was `a296c01`. First deployed code in three reports.
- Specs — **unchanged at 7** — `grep -c "test(" tests/*.spec.js` → `orders.spec.js:5`, `smoke.spec.js:2`.
- Restore-tested backup of `pokewatch.db` — **unchanged: none** — `grep -rniE "backup|litestream|restore" .github/workflows/ server.js package.json` → one hit, an unrelated comment at `server.js:1064`.
- Report payload size — was **52,783 B**, now **15,646 B** (−70%) — `git show origin/reports:business/daily/2026-09-15.md | wc -c`.
- `orders`/`users`/`products` row counts, and whether any report email was delivered — **not available in this environment**. `pokewatch.db` is gitignored and `.env` is droplet-only; I will not estimate either.

### 2. Highlights

- **Code shipped and reached production.** `9c1e53e` "Move Shop to the left of the nav bar" — one line at `src/components/TabNav.jsx:17`, `[...PRIVATE_TABS, SHOP_TAB]` → `[SHOP_TAB, ...PRIVATE_TABS]`. Deploy run #23 `conclusion: success`, finished 2026-09-15T18:06:12Z (https://github.com/StephanKotin/pokewatch/actions/runs/35005218161, retrieved 2026-09-16). The test gate passed, so this is live.
- **Correcting the commit message's own evidence.** It defends the change with "the default landing tab is unchanged… via App.jsx's `setActiveTab(user ? 'portfolio' : 'store')`". That expression sits at `src/App.jsx:235` and is the **logo-click** handler, not initial routing. The landing tab is `useState(() => tabFromPath(window.location.pathname))` (`src/App.jsx:67`), and `/` maps to `portfolio` at `src/App.jsx:29` for everyone — `user` is never consulted. **The conclusion holds; the mechanism cited is the wrong one**, and it would have misdirected the next person who tried to change the landing tab.
- **No verifier evidence exists for this UI change**, stated plainly so its absence is not read as a pass: it landed direct on `master` from a laptop (`git show 9c1e53e` → author Stephan Kotin, 2026-09-15 13:04 -0500), and `tests/smoke.spec.js` asserts nothing about nav order. The green suite proves the app still boots and signs in, not that the tabs reordered correctly.

### 3. Headwinds / blockers

1. **Blocking, 4th report running — no restore-tested backup.** Query in §1. Blocked on a human: a droplet-side drill no diff will ever show done. It caps the architecture composite at `min(D,R,S)+2 = 5` regardless of anything else built.
2. **Blocking — report email delivery still unconfirmed** for every weekday run since 09-14; I cannot observe the count from here, only that nothing in the repo records a send. Every failure path logs and returns silently (`emailDailyReport`, `server.js:1862-1906`). Cleared by a human checking an inbox or `journalctl -u pokewatch | grep '\[report\]'`.
3. **Blocking, unfixed since 09-14 — `.claude/skills/cto/SKILL.md:17` says `emailDailyReport` runs `30 12 * * 1-5`; `server.js:1918` says `0 13 * * 1-5`.** Wrong by 30 minutes in exactly the direction that misdirects a debug of blocker 2.
4. **Looming, unchanged:** Stripe MCP unauthorized in this session; second-brand hostname still unpointed, and a second origin turns `cors({ origin: APP_BASE_URL })` into a list.

### 4. Learnings

- **A commit message's conclusion can be right while its cited mechanism is wrong**, and the citation is the part the next reader reuses (§2). **Behavioural change:** when a commit message cites a file or expression as its safety argument, open that location before accepting the argument.
- **Last report's Gmail-clipping prediction was wrong, in the useful direction.** I forecast that one more report of that shape would clip at ~102 KB; the payload instead fell 52,783 B → 15,646 B (§1), and a 1,135-byte `business/daily/2026-09-15-digest.md` now exists on `origin/reports`. The size risk is **withdrawn, not re-filed**. The digest is written by the routine and is *not* read by the app: `emailDailyReport` still resolves exactly one path, `business/daily/${date}.md` (`server.js:1872`) — last report's finding that no digest fallback exists in code remains true.
- `CONTESTED: nav order and landing tab now disagree — signed-in users see Shop first (src/components/TabNav.jsx:17) but opening / still lands them on Portfolio (src/App.jsx:29). My position: pick one deliberately rather than letting a one-line nav tweak imply a default-tab change it did not make. Owners of the answer are cpo and vp-design, not me.`

### 5. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-15" origin/master -- .claude/` → empty. The one commit in the window touched only `src/`.
**Proposed:** (1) **re-filed, unchanged** — delete the `30 12 * * 1-5` literal at `.claude/skills/cto/SKILL.md:17` and keep the `grep -n "cron.schedule" server.js` recipe already beside it (blocker 3). (2) **new** — add to `daily-report` SKILL.md §2, alongside the existing `git show --stat` rule: a mechanism a commit message cites is verified at that location before the claim resting on it is repeated. (3) No new agent or worker warranted this window.

**Architecture impact: none.** `9c1e53e` reorders an array in one JSX component — no route, table, column, dependency, env var, deploy step or auth path. **Manual steps no diff will show, unchanged:** the backup-and-restore drill; `REPORT_EMAIL_TO` and `RESEND_API_KEY` in `.env` on the droplet; the second brand's DNS record and Caddyfile edit; the Stripe MCP authorization.

---

## Product — 2026-09-16

### 1. Progress since the last report

- Commits since anchor (`business/daily/2026-09-15.md`) — one: `9c1e53e`
  "Move Shop to the left of the nav bar" — `git log --oneline
  --since="2026-09-15" origin/master`.
- `business/product/roadmap.md` — still absent — `ls business/product/`.
- `business/product/specs/` — still absent — same command.
- Order/product/user/watchlist counts — `not available in this environment`
  (no access to `pokewatch.db`; it lives only on the droplet).

### 2. Highlights

- `9c1e53e` reorders the signed-in nav so Shop (storefront) sits ahead of
  Portfolio (collector) — `git show --stat 9c1e53e`: one file,
  `src/components/TabNav.jsx`, 1 line changed. Cross-product call: it makes
  the storefront the first tab a returning collector user sees, though login
  still lands on Portfolio per the commit's own description of
  `App.jsx`'s `setActiveTab` (not independently re-verified against
  `App.jsx` this cycle — engineering's section above did re-verify it and
  found the cited mechanism wrong, though the conclusion held). Signed-out
  visitors unaffected — they already saw Shop only.

### 3. Headwinds / blockers

- No spec preceded `9c1e53e` — it shipped as a direct one-line commit, not
  routed through `cto`. Not blocking today (small, reversible, no data or
  auth path touched), but it is the shape of decision the spec process
  exists to catch before ship.
- `business/product/roadmap.md` and `specs/` remain absent — no human has
  greenlit standing up the roadmap/spec machinery yet. Blocked on a human.

### 4. Learnings

- CONTESTED: `9c1e53e` decided a cross-product ordering question
  (storefront-first for signed-in collector users) without a spec. My
  position: acceptable this once given the tiny, reversible diff, but the
  next nav-ordering or default-tab change should route through `cto` first.
  No one has staked the opposing position yet.
- `git show --stat` on the single commit confirmed the diff was exactly
  1 file / 1 line — worth doing before trusting a commit subject line.

### 5. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-15" origin/master
-- .claude/` is empty.

**Proposed:** none this cycle.

---

## Marketing — 2026-09-16

### 1. Progress since the last report
- Commits since last report — was 0, now 1 (`9c1e53e`) — `git log --oneline --since="2026-09-15" origin/master`.
- `brand.md` — still does not exist — `find business -iname brand.md` returns nothing; unchanged since `business/daily/2026-09-15.md` (reports branch), which reported the same absence.
- Order/revenue/signup counts — not available in this environment (no access to `pokewatch.db`, which is gitignored and lives only on the droplet); unchanged caveat from prior reports.

### 2. Highlights
- `9c1e53e` "Move Shop to the left of the nav bar" — reorders the signed-in nav so the Shop tab (tcgoftexas.com) now renders first in `TabNav.jsx` — verified via `git show --stat 9c1e53e`: `src/components/TabNav.jsx | 2 +-`, 1 insertion/1 deletion. Verified independently, not just from the commit message: the `/` landing tab is unaffected for signed-in users — `tabFromPath()` in `src/App.jsx:47-51` falls back to `'portfolio'` for any path not in `TAB_PATHS`, including `/`. No storefront copy, price, or feature claim changed by this commit.

### 3. Headwinds / blockers
- **`brand.md` still absent** — no voice-of-record file exists to draft campaign copy against; blocks any new copy beyond what has already shipped. Not blocked on anyone else; carried forward from the 2026-09-15 report.
- **No campaign assigned this window.** Blocked on a human decision.
- Any pricing or card-fact copy still needs to route through `cfo` (`products`/`orders` tables) or the `catalogue-sync` ground rules respectively — moot this window since no new asset was drafted.

### 4. Learnings
- `9c1e53e` puts Shop ahead of Portfolio in the signed-in nav, which changes discoverability of tcgoftexas.com among existing PokéWatch users. I have no click-through or order data in this environment to say whether that helps — those counts are not available here. Recording this as an open question for `cfo` once order data is reachable, not as a claimed win.

### 5. Skill and agent updates
**Automated:** none — `git log --oneline --since="2026-09-15" origin/master -- .claude/` is empty.
**Proposed:** none new this window.

---

## Hotly debated topics

Written by the assembler from flagged `CONTESTED:` lines; contributors did not
author this section.

### 1. Does Shop-first nav need to be reconciled with the Portfolio landing tab?

**Raised by:** `cto` (CONTESTED, engineering §4). **Related, not identical,
flag from `cpo`** (product §4) — see topic 2.

- **`cto`:** signed-in users now see Shop ahead of Portfolio in the nav
  (`src/components/TabNav.jsx:17`), but opening `/` still lands them on
  Portfolio (`src/App.jsx:29`). Pick one deliberately rather than letting a
  one-line nav tweak imply a default-tab decision it did not actually make.
  Names `cpo` and `vp-design` as owners of the answer.
- **No counter-position filed.** No design executive is fielding a section
  this cycle (`head-of-design.md` absent — see intro), so there is currently
  no one on the design side to take the other side of this.
- **What would settle it:** `cpo` (with `vp-design` once that role exists in
  this checkout) states whether Shop-first nav + Portfolio-first landing is
  the intended combination, or whether one of the two should change to match.

### 2. Should a cross-product nav-ordering change route through a spec before shipping?

**Raised by:** `cpo` (CONTESTED, product §4).

- **`cpo`:** `9c1e53e` decided a cross-product ordering question without a
  spec. Acceptable this once given the tiny, reversible diff, but the next
  nav-ordering or default-tab change should route through `cto` first.
- **No counter-position filed.** `cto`'s section does not take a process
  position, only flags the outcome question above.
- **What would settle it:** whether `cto` agrees the process gap is worth
  closing, or judges the existing review path (this diff shipped without a
  `verifier` pass either — engineering §2) already sufficient for changes
  this small.

## Blocked on a human

- **No restore-tested backup of `pokewatch.db`** — 4th report running it as a
  blocker; caps the architecture composite at `min(D,R,S)+2 = 5`. (engineering)
- **Report-email delivery unconfirmed** since 2026-09-14 — needs a human to
  check an inbox or `journalctl -u pokewatch`. (engineering)
- **Stripe MCP still unauthorized** — needs an interactive session. (engineering)
- **Second-brand hostname** — DNS record and Caddyfile edit, neither in the
  repo. (engineering)
- **`business/product/roadmap.md` and `specs/` still not stood up** — no human
  has greenlit the process yet. (product)
- **No marketing campaign assigned this window.** (marketing)

# Company daily report — 2026-09-23

**Report #2.** Anchor: `business/daily/2026-09-12.md` (11 days — no report ran
between). Window for all git evidence: `--since="2026-09-12"`.

**Contributing:** engineering, product, marketing. Design is not in this run —
`.claude/agents/head-of-design.md` does not exist in this checkout yet, so the
design org has not reached `master`. `vp-research` was removed from the roster.
`cfo` and `vp-support` remain off-roster pending their triggers.

**Shared evidence (gathered once, handed to all three contributors):**
`git log --oneline --since="2026-09-12"` → one commit, `9c1e53e`.
`git log --oneline --since="2026-09-12" -- .claude/` → empty.
`git status --short` → empty (clean tree). Commit `a296c01` (2026-09-12) is the
anchor report's own creation, not a delta, and was excluded on that basis by
all three contributors.

**Note on this run's brief:** the brief instructed contributors to treat
`business/tenets.md` and every `business/*/goals.md` as deleted 2026-09-15 and
under rebuild, and to not read or cite them. All three complied. Engineering
additionally checked whether that premise holds and found it does not in this
checkout — see Hotly debated topics §2 below.

---

## Engineering — 2026-09-23

Window: 2026-09-12 → 2026-09-23. One commit (`9c1e53e`), no `.claude/` commits, clean tree.

### 1. Progress since the last report

- Commits in window — was 9, now 1 — `9c1e53e`
- Test count — unchanged at 7 — `grep -c "test(" tests/*.spec.js` → `orders.spec.js:5`, `smoke.spec.js:2`
- CI deploy gate — unchanged, intact — `.github/workflows/deploy.yml:21` `needs: test`
- Commits touching `server.js`, `package.json`, `tests/`, `.github/`, `.gitignore` — 0 — `git log --oneline --since=2026-09-12 -- <those paths>`
- Restore-tested backup — unchanged, none — grep for `backup|litestream|\.bak|restore` over `.github/workflows/ server.js package.json` → one unrelated comment, `server.js:1064`
- Users / orders / products counts — not available in this environment

### 2. Highlights

- `9c1e53e` — Shop tab moved first in the signed-in nav. `git show --stat`: `src/components/TabNav.jsx | 2 +-`, one line (`[...PRIVATE_TABS, SHOP_TAB]` → `[SHOP_TAB, ...PRIVATE_TABS]`). Signed-out nav and default landing tab unchanged. Architecture impact: none.

### 3. Headwinds / blockers

- Blocking, unchanged: no restore-tested backup of the production SQLite file — same grep as section 1. Blocked on a human; a droplet-side step no diff will show.
- Resolved: 48 MB untracked `src/music/` from the last report — `ls -d src/music` → no such directory; `git log --all -- src/music` → 0 commits, so it never entered history.
- Resolved: uncommitted agent layer from the last report — committed in `a296c01` (anchor baseline); `git status --short` now empty.
- Looming, unchanged: two-brand client half still waits on a second hostname — no commit in window touches it (`git log` above).
- CONTESTED: this run's brief says the governance and goals files under `business/` were deleted 2026-09-15. Git disagrees: `git ls-files business/` still lists them, they exist on disk, their last commit is `a296c01` (2026-09-12), and the tree is clean. I did not read or modify them. Position: either the deletion happened somewhere other than this checkout or it has not happened; a human should confirm before the rebuild relies on it.

### 4. Learnings

- A brief's description of repo state is not evidence either — one `git ls-files` contradicted a stated deletion. Behaviour change: I verify existence claims in the brief the same way I verify commit characterizations.

### 6. Skill and agent updates

- Automated: no `.claude/` commits in window — `git log --oneline --since=2026-09-12 -- .claude/` → empty.
- Proposed: none.

---

## Product — 2026-09-23

### 1. Progress since the last report
- First Product section in `business/daily/`: `2026-09-12.md` contains only
  Engineering and Design sections, no Product, so there is no prior
  product-specific baseline. `business/product/` holds only `goals.md` — no
  `roadmap.md`, no `specs/` (`find business/product -maxdepth 2` → single file).
- One commit in the window (`--since="2026-09-12"`) touches a product-owned
  surface: `9c1e53e`. See Highlights.

### 2. Highlights
- `9c1e53e` ("Move Shop to the left of the nav bar", `src/components/TabNav.jsx`,
  1 line changed) reorders the signed-in nav so Shop (tcgoftexas.com) sits
  ahead of Portfolio (PokéWatch) — a real cross-product call: it changes which
  brand a signed-in collector meets first. Default landing tab is untouched
  (`setActiveTab(user ? 'portfolio' : 'store')` per the commit message) and
  `PUBLIC_TABS`/gating are unchanged, so blast radius is small.
  CONTESTED: my position — this is exactly the kind of call this role's spec
  format exists to route (which product, whether it touches the other), and it
  shipped as a one-line diff with no spec in `business/product/specs/` (empty)
  and no supporting citation in `business/research/` (contains only
  `goals.md`). Not asking for a revert — it's reversible in one line — flagging
  so it isn't read as validated user demand.

### 3. Headwinds / blockers
- No research on file to test demand claims against: `ls business/research`
  → only `goals.md`. Any future "users want X" claim needs a `vp-research`
  scan first.
- Storefront/collector usage counts (orders, products, watchlist rows) are not
  available in this environment — `pokewatch.db` is gitignored and droplet-only.

### 4. Learnings
- `business/product/` has no `roadmap.md` and no `specs/` yet (`find
  business/product -maxdepth 2` confirms). Once domain files are rebuilt, the
  first roadmap entry should record what's already live, not start blank.

### 6. Skill and agent updates
No `.claude/` commits in the window
(`git log --oneline --since="2026-09-12" -- .claude/` → empty). Nothing to
report.

---

## Marketing — 2026-09-23

### 1. Progress since the last report
- First marketing section filed — `business/daily/2026-09-12.md` has only
  `## Engineering` and `## Design` headings (grep `## Marketing` → no match).
- No commits touched `business/marketing/` in the window:
  `git log --oneline --since="2026-09-12" -- business/marketing/` → empty.
- No product/order/price figures pulled — `pokewatch.db` is gitignored and
  absent from this sandbox; not available in this environment.

### 2. Highlights
- One commit in the window: `9c1e53e` ("Move Shop to the left of the nav
  bar", `src/components/TabNav.jsx`, 1 line) — for signed-in users, Shop now
  renders before Portfolio. Signed-out visitors already saw only Shop,
  unaffected. Default landing tab unchanged per the commit's own citation of
  `setActiveTab(user ? 'portfolio' : 'store')` in `src/App.jsx`.
  CONTESTED: Shop-first for signed-in users nudges a collector session
  toward the storefront, in tension with keeping the two audiences separate;
  counter-point is the landing tab itself didn't move. Flagging for the
  assembler — no rationale beyond the commit message was available.
- No other commit touched brand-facing copy or storefront pages this window.

### 3. Headwinds / blockers
- `business/marketing/brand.md` does not exist: `find business -iname
  "brand*"` (2026-09-23) returns nothing. This role's brief calls it "the
  voice of record" — with no file, drafts have no baseline to check against.
  Out of scope to create here; needs a human/`cpo` call on priority.
- No products or orders exist to claim anything about, per the 2026-09-12
  baseline (`products=0`, `orders=0`), unverifiable further here (no DB
  access). Promo/product copy is blocked on a product existing.

### 4. Learnings
- First report for this domain — nothing recurring yet to compare against.
- `brand.md`'s absence was found by checking, not assumed from the brief's
  phrasing — a brief describing a file isn't evidence it exists.

### 6. Skill and agent updates
- No `.claude/` changes in the window:
  `git log --oneline --since="2026-09-12" -- .claude/` → empty.

---

## Hotly debated topics (assembled)

Written by the assembler from flagged `CONTESTED:` items. Contributors did not author this section.

### 1. Should the Shop-first nav reorder (`9c1e53e`) have shipped without a spec or research citation?

**Raised by:** `cpo` (CONTESTED, Product §2), reinforced by `cmo` (CONTESTED,
Marketing §2) from a brand-separation angle. No counter-position filed —
`cto` implemented and reported the commit without comment on process.

- **`cpo`:** this is exactly the kind of cross-product call the spec process
  exists to route, and it shipped as a one-line diff with no spec in
  `business/product/specs/` (empty) and no citation in `business/research/`
  (only `goals.md`). Not asking for a revert — flagging so it isn't read as
  validated demand.
- **`cmo`:** Shop now renders before Portfolio for signed-in users, in tension
  with keeping the two brands' audiences separate. Self-noted counter-point:
  the default landing tab didn't move, so blast radius is small.
- **What would settle it:** a retroactive one-line rationale for the ordering
  call, and a decision on whether nav-ordering changes need a product spec
  going forward. Neither exists yet.

### 2. Do `business/tenets.md` and the `business/*/goals.md` files exist, or were they deleted?

**Raised by:** `cto` (CONTESTED, Engineering §3).

- This run's brief stated they were deleted 2026-09-15 and are being rebuilt.
  Git disagrees in this checkout: `git ls-files business/` still lists them,
  they are present on disk, last touched by `a296c01` (2026-09-12), and the
  working tree is clean.
- No counter-position filed. `cpo` and `cmo` independently followed the
  brief's instruction not to read or cite them, but neither investigated
  whether the premise was true.
- **What would settle it:** a human confirming whether the deletion happened
  on a different branch or checkout, is still pending, or the brief's premise
  was simply incorrect.

---

## Blocked on a human

- **No restore-tested backup** of the production SQLite file — carried,
  unchanged since the anchor report. A droplet-side step; no diff will show it.
- **Second-brand hostname/DNS** — the two-brand client work still waits on it;
  neither is in this repo. Carried, unchanged.
- **`business/tenets.md` / `business/*/goals.md` existence** — this run's
  brief says they were deleted 2026-09-15; this checkout shows them present
  and last touched 2026-09-12. Needs a human to reconcile before the
  "rebuild" proceeds on either premise. See Hotly debated topics §2.

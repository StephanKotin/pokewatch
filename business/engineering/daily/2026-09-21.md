## Engineering — 2026-09-21

**Report #7.** Owner `cto`. Anchor `business/daily/2026-09-18.md` (report #6). Window: `--since="2026-09-18"`, branch `reports`. **Correction to the shared facts handed to me:** the window was passed as empty; my own run returns one commit, `a3cc490` "Daily report 2026-09-18". `git show --stat a3cc490` → 5 files, 318 insertions, **all under `business/`** (`daily/2026-09-18.md` + digest, engineering/, marketing/, product/). So: zero *code* commits, not zero commits. Stat output wins over a characterization, per the skill. By exception below.

### 1. Progress since the last report

- Code commits — was **0**, now **0** — `git log --oneline --since="2026-09-18"` → `a3cc490` only, whose stat above is `business/`-only. Third consecutive window with no code change.
- Specs — **unchanged, 7** — `grep -c "test(" tests/*.spec.js` → `orders.spec.js:5`, `smoke.spec.js:2`.
- Restore-tested backup of `pokewatch.db` — **unchanged: still none** — `grep -rniE "backup|litestream|restore" .github/workflows/ server.js package.json` → one hit, the unrelated comment at `server.js:1064`.
- Report payload size — was **12,325 B**, now **13,064 B** (**+6%**) — `wc -c business/daily/2026-09-18.md`. **Reverses three consecutive falls.**
- Cron-literal mismatch — reported **cleared** in #6; **it is not cleared.** See §2.
- Row counts, email-delivery counts, droplet state — **not available in this environment**; `pokewatch.db` is gitignored and droplet-only. Not estimated.

### 2. Highlights

- **Correcting report #6: the cron-literal fix it claimed does not exist.** `business/engineering/daily/2026-09-18.md:11` says "cleared by me this window" and `:16` records it as a working-tree edit to `.claude/skills/cto/SKILL.md:17`. Re-run today: `grep -n "cron.schedule" server.js` → `server.js:1918: cron.schedule('0 13 * * 1-5', emailDailyReport, { timezone: 'UTC' })`; `grep -n "30 12" .claude/skills/cto/SKILL.md` → `:17` still carries the literal `30 12 * * 1-5`. `git status --short` → empty, so the edit is not even pending. `git log --oneline -3 -- .claude/skills/cto/SKILL.md` → newest is `a296c01`, which predates #6. The edit was made in an ephemeral sandbox, was not included in the report-assembly commit (which touches only `business/`), and evaporated. **#6's claim was inaccurate; the mismatch is live.**
- Nothing shipped and nothing regressed in code — zero code commits, per §1.

### 3. Headwinds / blockers

1. **Blocking, 7th report running — no restore-tested backup of `pokewatch.db`.** Re-queried today, result in §1. Needs a human droplet-side drill. Still caps the architecture composite at `min(D,R,S)+2 = 5`.
2. **Blocking, 6th report — report-email delivery unconfirmed since 2026-09-14.** Re-queried: `grep -n "cron.schedule" server.js` → `1800` (`scanPrices`) and `1918` (`emailDailyReport`); every failure path in `emailDailyReport` still logs and returns silently. Cleared only by a human checking the inbox or `journalctl -u pokewatch | grep '\[report\]'`.
3. **Re-filed with corrected history — cron-literal mismatch, claimed-fixed-but-wasn't.** Evidence in §2; `server.js:1918` says `0 13`, `.claude/skills/cto/SKILL.md:17` says `30 12`. **I am not re-applying the edit this window**, and not because it is hard: the file is outside `business/`, my brief for this run is write-only to this section file, and — decisively — repeating the identical working-tree edit in an identical ephemeral sandbox reproduces the identical evaporation. Clearing this needs a commit on `reports` that includes `.claude/`, or a human edit. Not a fourth attempt at the same gesture.
4. **Open, day 6 — nav order vs landing tab** (filed "day 3" at `business/engineering/daily/2026-09-18.md:23`). Re-queried, both unchanged: `src/components/TabNav.jsx:17` → `const tabs = user ? [SHOP_TAB, ...PRIVATE_TABS] : [SHOP_TAB];`; `src/App.jsx:50` → `return entry ? entry[0] : 'portfolio';` with `portfolio: '/'` at `src/App.jsx:29`. Zero code commits since, so nothing has decided it. Owners: `cpo`, `vp-design`.
5. **Looming, unchanged:** Stripe MCP still unauthorized — this session's own startup banner lists `stripe` under servers requiring authentication, and the session is non-interactive. Second-brand hostname still unpointed (DNS + Caddyfile, neither in the repo).

### 4. Learnings

- **An uncommitted edit is not a delivered change, and #6's format let it read as one.** #6 filed the fix under Highlights with the caveat "Uncommitted at time of writing" — accurate then, wrong three days later, because the assembly commit stages only `business/`. **Behavioural change:** I do not report a file outside `business/` as changed unless `git log -- <path>` shows a commit containing it. "Edited in the working tree" goes in Blockers, never Highlights.
- **#6's own Learning — "an item I can clear in one edit gets cleared in the same report" — was the trap, not the fix.** It optimised for making the edit and not for delivering it, and converted three honest blocker entries into one false Highlight. **Behavioural change:** when a fix is one edit, the report names who commits it, or it is filed as blocked-on-a-human.
- **The shared-facts hand-off drifted once today** (see header): the window was characterized as zero commits and contains one. Cost to catch: one `git log` plus one `git show --stat`. Re-running the anchor query is not optional.
- `CONTESTED: nav order and landing tab still disagree (§3.4), day 6 — signed-in users see Shop first but opening / lands on Portfolio. Position unchanged: decide the default tab deliberately rather than let a one-line nav tweak imply a change it did not make. Re-filed because nothing in this window addressed it.`
- `CONTESTED: whether a contributor section may report an edit outside its own directory as applied. My position: it may not, because the assembly commit stages only business/ — today's §2 is the proof. Either the assembly commit widens to carry contributor edits under .claude/, or contributors are fenced to reporting and a human applies. Who may disagree: the assembler, on the grounds that widening the commit lets five concurrent agents land code-adjacent files with no review.`

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-18" -- .claude/` → empty (re-run by me, not taken on trust).
**Applied by me:** none. The `.claude/skills/cto/SKILL.md:17` edit that #6 reported as applied is not present (§2), and I did not re-apply it (§3.3). My only write this run is this file.
**Proposed:** (1) the `daily-report` skill should state that a contributor may report an edit outside `business/` as *applied* only once a commit containing it exists — otherwise it is a blocker. Second consecutive report proposing a `daily-report` rule, and the first with a concrete failure behind it. (2) **carried, unactioned** — the §2 rule that a mechanism cited by a commit message is opened before the claim resting on it is repeated. (3) No new agent or worker warranted.

**Architecture impact: none.** Zero code commits; my only write is one markdown file under `business/`, touching no route, table, column, dependency, env var, deploy step or auth path. **Manual steps no diff will show, unchanged:** the backup-and-restore drill; `REPORT_EMAIL_TO` and `RESEND_API_KEY` in `.env` on the droplet; the second brand's DNS record and Caddyfile edit; the Stripe MCP authorization.

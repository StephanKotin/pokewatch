## Engineering — 2026-09-24

### 1. Progress since the last report

- Commits in window — was 9, now 1 — `git log --since="2026-09-12"` → `9c1e53e` only
- Uncommitted work — was 2 modified + 17 untracked, now 0 — `git status --short` empty; the agent layer landed in `a296c01` (2026-09-12, the commit that carried the last report)
- Untracked audio in `src/music/` — was 48 MB, now absent — `ls -d src/music` → no such file
- Test count — unchanged at 7 — `grep -c "test(" tests/*.spec.js` → `orders:5`, `smoke:2`
- CI deploy gate — unchanged, intact — `.github/workflows/deploy.yml:21` `needs: test`
- Restore-tested backup — unchanged, none — `grep -rniE "litestream|\.backup\(" .github/workflows/ server.js package.json` → no hits
- Users, orders, products row counts — not available in this environment (no `pokewatch.db`)

### 2. Highlights

- `9c1e53e`: one line changed in `src/components/TabNav.jsx`. Signed-in users now see Shop first in the nav. It is the only change in the window. Putting Shop first was product's decision, so I am not counting it as an engineering highlight. My only engineering claim: per the commit message, the default landing tab is unchanged. Architecture impact: none.

### 3. Headwinds / blockers

- Still no restore-tested backup of the droplet's SQLite file (see grep above). This caps the architecture composite at 5, and nothing built in this window can lift it. Blocked on a human: someone has to choose a backup target and run one restore on the droplet. No diff will show that step.
- The brief says tenets and goals were deleted on 2026-09-15, but this checkout still has `business/tenets.md` and all six `goals.md` files. `git status` is clean, and no commit after `a296c01` touches them. So the deletion did not happen in this repo, or it has not been pushed yet. I have not read them. A human should confirm where the rebuild is happening.
- The Stripe MCP server did not connect this session: `ERR_PROXY_TUNNEL: 403 Forbidden`, from the startup notice on 2026-09-24. Live Stripe state still cannot be read from a sandbox session.

### 4. Learnings

- `git log --since="2026-09-12"` leaves out `a296c01`, even though that commit is dated the anchor day. When git gets a date with no time, it fills in the current clock time, not midnight. A commit that lands on the anchor date can therefore fall between two reports. Behavioural change: I now anchor the window on the last report's commit SHA (`a296c01..HEAD`) instead of a date.

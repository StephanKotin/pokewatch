## Engineering — 2026-09-17

**Report #5.** Owner `cto`. Anchor `business/daily/2026-09-16.md`. Window: since 2026-09-16. Re-verified rather than trusted: `git log --oneline --since="2026-09-16" origin/master` → empty; same query scoped `-- .claude/` → empty; `git status --short` → empty; `git log --oneline -3 origin/master` → head still `9c1e53e`, the commit already reported on 09-16. **No code changed in this window.** Reporting by exception below.

### 1. Progress since the last report

- Commits on `origin/master` — was **1**, now **0** — query above. `9c1e53e` is unchanged as head and was covered in the 09-16 report; not re-reported here.
- Specs — **no change since the last report, still 7** — `grep -c "test(" tests/*.spec.js` → `orders.spec.js:5`, `smoke.spec.js:2`.
- Restore-tested backup of `pokewatch.db` — **no change: still none** — `grep -rniE "backup|litestream|restore" .github/workflows/ server.js package.json` → one hit, the same unrelated comment at `server.js:1064`.
- Report payload size — was **15,646 B**, now **13,998 B** (−11%) — `git show HEAD:business/daily/2026-09-16.md | wc -c`. Second consecutive fall; `business/daily/2026-09-16-digest.md` exists on this branch (`git ls-tree --name-only HEAD business/daily/`).
- Row counts, email-delivery counts, droplet state — **not available in this environment**; `pokewatch.db` is gitignored and droplet-only. Not estimated.

### 2. Highlights

- **Nothing shipped, and nothing regressed.** The one genuinely new observation is the payload trend in §1: the report format has now shrunk two windows running without losing citations, which retires the Gmail-clipping risk withdrawn on 09-16 rather than merely deferring it.
- No verifier or release-guard work was warranted: with an empty diff there is nothing to prove in the running app.

### 3. Headwinds / blockers

1. **Blocking, 5th report running — no restore-tested backup.** Re-checked today, not assumed; query in §1. Still blocked on a human droplet-side drill. Still caps the architecture composite at `min(D,R,S)+2 = 5`.
2. **Blocking, 4th report — report email delivery still unconfirmed.** Re-checked: every failure path in `emailDailyReport` still logs and returns silently (`grep -n "cron.schedule" server.js` → `server.js:1918`). Cleared only by a human checking an inbox or `journalctl -u pokewatch | grep '\[report\]'`.
3. **Blocking, unfixed since 09-14 (4 days) — cron literal mismatch.** Confirmed still wrong today: `.claude/skills/cto/SKILL.md:17` says `30 12 * * 1-5`; `server.js:1918` says `cron.schedule('0 13 * * 1-5', emailDailyReport, { timezone: 'UTC' })`. 30 minutes off, in the direction that misdirects a debug of blocker 2.
4. **Open, unresolved by anything in this window — nav order vs landing tab.** `src/components/TabNav.jsx:17` → `user ? [SHOP_TAB, ...PRIVATE_TABS]`; `src/App.jsx:50` still returns `'portfolio'` for `/`. Zero commits since, so nothing has decided it. Owners: `cpo`, `vp-design`.
5. **Looming, unchanged:** Stripe MCP unauthorized in this session; second-brand hostname still unpointed.

### 4. Learnings

- **A carried-forward blocker has to be re-queried, not re-typed.** All four of today's carry-forwards were re-run against the working tree before being restated; the cron mismatch at `SKILL.md:17` in particular would read identically whether it had been fixed or not. **Behavioural change:** a blocker restated without a fresh query is a claim about yesterday, not today, and does not get written.
- **An empty window is cheap to report and expensive to pad.** Today's section cost five greps; the temptation to re-derive `9c1e53e`'s `--stat` for a second consecutive report would have produced volume and zero new information.
- `CONTESTED: nav order and landing tab still disagree (§3.4) — signed-in users see Shop first but opening / lands on Portfolio. My position is unchanged: decide the default tab deliberately rather than let a one-line nav tweak imply a change it did not make. Re-filed because nothing in this window addressed it, not because it is newly contested.`

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-16" origin/master -- .claude/` → empty.
**Proposed:** (1) **re-filed for the 3rd time, unchanged** — delete the `30 12 * * 1-5` literal at `.claude/skills/cto/SKILL.md:17` and keep the `grep -n "cron.schedule" server.js` recipe already beside it (blocker 3). It is a one-word edit that has now outlived three reports, which is itself the argument for making it. (2) **carried, unactioned** — the `daily-report` §2 rule that a mechanism cited by a commit message is opened before the claim resting on it is repeated. (3) No new agent or worker warranted.

**Architecture impact: none.** Zero commits in the window — no route, table, column, dependency, env var, deploy step or auth path changed. **Manual steps no diff will show, unchanged:** the backup-and-restore drill; `REPORT_EMAIL_TO` and `RESEND_API_KEY` in `.env` on the droplet; the second brand's DNS record and Caddyfile edit; the Stripe MCP authorization.

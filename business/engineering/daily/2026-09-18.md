## Engineering — 2026-09-18

**Report #6.** Owner `cto`. Anchor `business/daily/2026-09-17.md`. Window: since 2026-09-17. Re-verified, not trusted: `git log --oneline --since="2026-09-17" origin/master` → empty; same scoped `-- .claude/` → empty; `git status --short` → empty (before my write); `git log --oneline -1 origin/master` → `9c1e53e`, already reported in #4 and re-confirmed in #5, so not re-reported here. **Zero commits in this window.** By exception below.

### 1. Progress since the last report

- Commits on `origin/master` — was **0**, now **0** — query above. Second consecutive empty window.
- Specs — **no change, still 7** — `grep -c "test(" tests/*.spec.js` → `orders.spec.js:5`, `smoke.spec.js:2`.
- Restore-tested backup of `pokewatch.db` — **no change: still none** — `grep -rniE "backup|litestream|restore" .github/workflows/ server.js package.json` → one hit, the same unrelated comment at `server.js:1064`.
- Report payload size — was **13,998 B**, now **12,325 B** (−12%) — `git show HEAD:business/daily/2026-09-17.md | wc -c`. Third consecutive fall; `business/daily/2026-09-17-digest.md` present (`ls business/daily/`).
- Cron-literal mismatch (blocker 3 on the last three reports) — **cleared by me this window**, see §2.
- Row counts, email-delivery counts, droplet state — **not available in this environment**; `pokewatch.db` is gitignored and droplet-only. Not estimated.

### 2. Highlights

- **The cron-literal mismatch is fixed.** Confirmed still wrong first (`grep -n "cron.schedule" server.js` → `server.js:1918: cron.schedule('0 13 * * 1-5', …)`; `.claude/skills/cto/SKILL.md:17` still read `30 12 * * 1-5`), then edited: the literal is deleted and the line now points at the `grep -n "cron.schedule" server.js` recipe already beside it. Working-tree edit to `.claude/skills/cto/SKILL.md:17`, **outside `business/`** — the assembler needs to know this branch carries one non-report file change. Uncommitted at time of writing.
- Nothing else shipped and nothing regressed — zero commits, per §1.

### 3. Headwinds / blockers

1. **Blocking, 6th report running — no restore-tested backup of `pokewatch.db`.** Re-queried today, not recalled; result in §1. Needs a human droplet-side drill. Still caps the architecture composite at `min(D,R,S)+2 = 5`.
2. **Blocking, 5th report — report-email delivery unconfirmed since 2026-09-14.** Re-queried: `grep -n "cron.schedule" server.js` → `server.js:1918`; every failure path in `emailDailyReport` still logs and returns silently. Cleared only by a human checking the inbox or `journalctl -u pokewatch | grep '\[report\]'`. Note the fix in §2 removes a wrong literal that would have misdirected exactly this debug.
3. **Open, day 3 — nav order vs landing tab.** Re-queried, both unchanged: `src/components/TabNav.jsx:17` → `const tabs = user ? [SHOP_TAB, ...PRIVATE_TABS] : [SHOP_TAB];`; `src/App.jsx:50` → `return entry ? entry[0] : 'portfolio';` with `portfolio: '/'` at `src/App.jsx:29`. Zero commits since, so nothing has decided it. Owners: `cpo`, `vp-design`.
4. **Looming, unchanged:** Stripe MCP unauthorized in this session (session banner, non-interactive); second-brand hostname still unpointed.

### 4. Learnings

- **A self-clearable item re-filed three times is evidence the filing is cheaper than the fix, which is the wrong incentive.** The cron literal cost one `Edit` today and cost three reports of prose before that. **Behavioural change:** an item I can clear in one edit gets cleared in the same report that would otherwise re-file it, or it is dropped from blockers entirely — it does not get a fourth appearance.
- **Re-querying caught nothing wrong today, and that is still the point.** All four carry-forwards were re-run against the working tree; three were unchanged and one had to be confirmed wrong before it could be fixed. The cost was five greps.
- `CONTESTED: nav order and landing tab still disagree (§3.3), day 3 — signed-in users see Shop first but opening / lands on Portfolio. Position unchanged: decide the default tab deliberately rather than let a one-line nav tweak imply a change it did not make. Re-filed because nothing in this window addressed it, not because it is newly contested.`

### 6. Skill and agent updates

**Automated:** none — `git log --oneline --since="2026-09-17" origin/master -- .claude/` → empty.
**Applied by me:** `.claude/skills/cto/SKILL.md:17` — the `30 12 * * 1-5` literal removed in favour of the existing `grep` recipe, per that skill's own "locations as grep recipes, not literals" rule. Uncommitted working-tree change on `reports`; **must not reach `master`** without the usual gate.
**Proposed:** (1) **carried, unactioned** — the `daily-report` §2 rule that a mechanism cited by a commit message is opened before the claim resting on it is repeated. (2) No new agent or worker warranted.

**Architecture impact: none.** Zero commits; the one edit is a one-line documentation correction touching no route, table, column, dependency, env var, deploy step or auth path. **Manual steps no diff will show, unchanged:** the backup-and-restore drill; `REPORT_EMAIL_TO` and `RESEND_API_KEY` in `.env` on the droplet; the second brand's DNS record and Caddyfile edit; the Stripe MCP authorization.

## Research — 2026-09-14

**Report #2** for the company report overall, but the **first section research
has ever filed**. `business/daily/2026-09-12.md`'s own header lists
"Contributing: engineering (`cto`), design (`vp-design`)" only — research did
not run in Report #1. So there is no prior research section to diff against;
everything below is a baseline, the same as it would be for a report #1.

**Anchor for code/repo evidence:** `a296c01` (Report #1's own commit).
`git log --oneline a296c01..HEAD` → empty, 0 commits. `git status --short` →
empty. Confirmed independently this session (not taken on trust) — see
section 7.

---

### 1. Goals and tenet coverage

**Company goals this domain moves:** `business/goals.md` — `STATUS: unset`
(`business/goals.md:6`). No human-directed company goals exist yet, so none
to restate or move against.

**Agent-developed goals** (`business/research/goals.md` —
`STATUS: unset`, line 8, unchanged since Report #1): no goals existed before
this session. One is proposed below, drawn from work actually done this
window, not invented for the section:

- **Establish and maintain a dated competitor-pricing baseline, one scan per
  market per month.**
  - Serves: `R1`
  - Baseline: **0** — `business/research/scans/` did not exist before this
    session (`ls business/research` before this run returned only
    `goals.md`; confirmed via `Bash` at session start).
  - Target: 1 scan per market (PokéWatch-side and tcgoftexas.com-side) per
    month, each with ≥2 independently corroborated findings.
  - Measured from: file count and finding count in
    `business/research/scans/`.
  - Current: **1 scan filed**, 2026-09-14, 2 queries, 2 findings — both
    single-primary-source (see section 3 caveat) —
    `business/research/scans/2026-09-14-competitor-pricing-scan.md`.
  - Status: active — not yet written to `goals.md` this way; this is the
    proposal, not a self-granted goal. I am filing it here per the skill
    ("agent-developed goals" is this agent's own file to maintain) and will
    keep tracking it whether or not a human formalizes it.

**PROPOSED FOR COMPANY GOALS:** none this period. Nothing found today changes
what gets built, charged, or shown to a customer — it is market context, not
a roadmap claim.

**Tenet coverage**, checked against `business/tenets.md` both ways:

- **Uncovered: `R2`.** No goal serves "track the frontier of research method
  itself" yet. `Last acted: never` (`business/tenets.md`, R2 block), and it
  is still `never` after this session — I ran a market scan (`R1` territory),
  not a research-tooling scan. Not claiming this as stale by cadence math
  (quarterly, tenets set 2026-09-12, so not yet due by date), just naming it
  uncovered.
- **`R1`** was also `Last acted: never` going into this session. I acted on
  it this window (see section 3) and have updated
  `business/tenets.md`'s R1 line to `2026-09-14` — the only edit made to
  that file, per the rule that only the acting agent updates its own
  `Last acted` line.
- **Shared `T2`** (weekly capability scan, cross-domain) is also
  `Last acted: never` in `business/tenets.md`. That is a shared tenet, not
  mine alone to close, and I did not run a capability/tooling scan this
  session (only a market-pricing scan) — naming it rather than claiming
  credit I didn't earn.
- Not claiming any tenet is past-cadence "stale" by strict date math: tenets
  were set 2026-09-12, two days before this report, so weekly/monthly/
  quarterly windows have not elapsed from a real baseline yet. "Never acted"
  and "stale" are different claims; I'm being precise about which one
  applies.

### 2. Progress since the last report

Research filed no section in Report #1, so there is no prior "since last
report" delta for this domain — every line below is the first recorded
value, not a change from a previous measurement:

- Research scans on file — was 0, now 1 —
  `business/research/scans/2026-09-14-competitor-pricing-scan.md` (file did
  not exist before this session).
- `business/research/goals.md` STATUS — unchanged, still `unset` —
  `business/research/goals.md:8`.
- `R1` `Last acted` — was `never`, now `2026-09-14` — `business/tenets.md`
  (edited this session; only line touched).
- `R2` `Last acted` — unchanged, still `never` — `business/tenets.md`.
- Code/repo state relevant to either market (pricing, plans, storefront
  tables) — **not available in this environment.** No access to
  `pokewatch.db` this session; any of that would need a query I cannot run
  here.

### 3. Highlights

- **First research scan ever filed**, 2026-09-14: two `WebSearch` queries,
  one per market, filed raw at
  `business/research/scans/2026-09-14-competitor-pricing-scan.md` before any
  interpretation was added here — keeping the raw/synthesis split the role
  calls for, even though `market-scanner` itself wasn't available to
  delegate to this session (no `Agent`/`Task` tool in this session's tool
  set — see section 4).

- **PokéWatch market — Observed, with a confidence caveat:** Collectr, a
  Pokemon/multi-TCG portfolio-tracking app, prices its paid tier at
  **$4.99/mo billed annually or $7.99/mo billed monthly**, with a free tier
  to start. Retrieved via `WebSearch`, 2026-09-14, surfacing
  `https://www.getcollectr.com/track`. **`WebFetch` on that URL returned
  `EGRESS_BLOCKED` this session**, so this is a search-engine-aggregated
  snippet of the vendor's own pricing page, not an independently fetched
  primary read. **1 source** (the vendor's own site, restated by the search
  engine plus one review-site mention that appears to cite the same
  number) — flagging this as **single-source** per the pre-report checklist.
  Inference, not observation: PokéWatch currently has no paid tier of its
  own to compare against (not checked this session — that would need
  `server.js`'s Stripe routes re-read, which I did not do; stating this as
  an open question, not a finding).

- **tcgoftexas.com market — Observed, with the same confidence caveat:**
  TCGplayer raised its standard Marketplace Seller commission from **10.25%
  to 10.75%**, plus a **2.5% + $0.30** transaction fee on the full order
  total (incl. tax), with the per-item commission cap raised to **$75**,
  effective **2026-02-10**. Retrieved via `WebSearch`, 2026-09-14, tracing to
  `https://seller.tcgplayer.com/blog/important-changes-to-tcgplayer-direct-minimum-pricing-and-marketplace-fees`.
  Several third-party fee calculators (`cardsynced.com`, `tcgdropradar.com`,
  `poketop10.com`, `tcgfeecalc.com`, `pulltrader.app`, `brandclickx.com`)
  restate the same figures, but all trace to the one TCGplayer announcement
  — **1 primary source**, restated widely, not independently corroborated.
  `WebFetch` on the primary URL also returned `EGRESS_BLOCKED`. Flagging as
  **single-source**.

**Confidence summary for this report's two findings:** both are single-source
(one primary origin each, restated by aggregators), both retrieved
2026-09-14 (so the "oldest source" for everything in this report is today —
there is no older corroborating data point on file, because no scan existed
before today), and neither was independently verified by direct fetch
because `WebFetch` was blocked for every domain tried this session. Treat
both as provisional until a session with working `WebFetch` re-confirms them
against the primary pages directly.

### 4. Headwinds / blockers

- **`WebFetch` is blocked in this sandbox for every external domain tried
  this session** (`www.getcollectr.com`, `seller.tcgplayer.com`,
  `tcgdropradar.com` all returned `EGRESS_BLOCKED`). This caps today's
  findings at "aggregator snippet," not "primary source read," which is
  below this role's normal bar. Not clearly blocked on a human — may be
  sandbox-specific rather than a standing restriction — but it blocks
  upgrading today's confidence level from this session. Whoever runs the
  next research window should retry `WebFetch` against the same two URLs and
  report whether it is still blocked.
- **No `Agent`/`Task` tool was available in this session's tool set**, so the
  raw-scan-then-synthesize split that `market-scanner` normally does had to
  be performed by `vp-research` directly (still kept as two separate
  artifacts — raw scan in `business/research/scans/`, synthesis here — but
  by one agent instead of two). Not blocking today's output, but worth
  naming since it's a deviation from the delegation model described in this
  agent's own brief.
- **Both `goals.md` files are `STATUS: unset`** (`business/goals.md:6`,
  `business/research/goals.md:8`) — there is no accepted goal to grade
  section 2 against yet beyond the one proposed in section 1. Blocked on a
  human accepting, editing or rejecting it.

### 5. Learnings

- **Research had no representation in Report #1 at all** — not "no change,"
  but "did not run." That's a different fact than the other four domains
  reported, and it means this domain's baseline starts today
  (2026-09-14), not 2026-09-12. Behavioral change: said this plainly in the
  header instead of letting "Report #2" imply continuity that doesn't exist.
- **`WebFetch` fails closed for competitor pricing/blog domains in this
  sandbox specifically** (three for three today) — a fact worth carrying
  into the next scan rather than rediscovering it. Behavioral change: try
  `WebFetch` first on one URL before planning a scan around it, and default
  to citing `WebSearch`'s own surfaced URLs with an explicit confidence
  caveat when it fails, rather than silently presenting an aggregator
  snippet as a primary read.
- **Capability scan (`T2`/`R2`): not run this window.** This was a market
  scan (`R1`), not a research-method/tooling scan. No finding to report for
  `R2` — recorded as an absence, not dressed up as a quiet result, per T2's
  own rule that "scanned, found nothing" and "did not scan" are different
  and both are honestly reportable, but only one of them is what happened.

### 6. Hotly debated topics

No `CONTESTED:` flags from this domain this period. (Section 6 is assembled
by the report assembler from flagged items and cross-section
contradictions; this agent does not author it.)

### 7. Skill and agent updates

**Automated** (`.claude/`-touching commits since the anchor): **none.**
`git log --oneline a296c01..HEAD -- .claude/agents/vp-research.md
.claude/skills/` returns empty, and `git log --oneline a296c01..HEAD` is
empty overall — confirmed directly this session, not taken from the prompt's
characterization. `.claude/agents/vp-research.md` and
`.claude/agents/market-scanner.md` were both created in `a296c01` itself
(`git log --oneline -- .claude/agents/vp-research.md` → `a296c01` only),
which is the commit Report #1 already reported on; nothing has touched
either file since.

**Proposed:** none this period beyond the goal proposal in section 1. Not
proposing a skill change for the `WebFetch` blocking yet — one session's
three-for-three failure isn't enough to conclude it's a standing property of
this environment rather than a transient proxy state; worth confirming once
more before writing a rule into a skill (T5's own bar: write down what was
*expensive* to learn, and this cost one blocked fetch attempt, not a
debugging session).

---

**Architecture impact: none.** This report and its accompanying scan file
are markdown under `business/`, which nothing in `src/` or `server.js`
imports or reads.

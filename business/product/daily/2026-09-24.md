## Product — 2026-09-24

### 1. Progress since the last report
- Commits since 2026-09-12 — was 0 known, now 1 (`9c1e53e`) — `git log --oneline --since="2026-09-12"`.
- Order/product/user/watchlist metrics — not available in this environment (no access to `pokewatch.db`, which is gitignored and lives only on the droplet).
- Everything else (specs drafted, roadmap sequence) — unchanged since 2026-09-12 — `git log --oneline --since="2026-09-12" -- .claude/` and `git log --oneline --since="2026-09-12" -- business/product/` both empty.

### 2. Highlights
- `9c1e53e` "Move Shop to the left of the nav bar" — `src/components/TabNav.jsx:17`, one line changed (`git show --stat 9c1e53e`). For signed-in users, tab order becomes `[Shop, ...PRIVATE_TABS]` instead of `[...PRIVATE_TABS, Shop]`; signed-out visitors see only Shop either way, so unaffected. This is a nav-order tweak, not a feature — no route, table, or column touched, so no scorecard entry is warranted. Worth flagging only because it nudges signed-in collector users toward the storefront tab first; the default landing tab is unchanged (`App.jsx`'s `setActiveTab(user ? 'portfolio' : 'store')`, per the commit body), so this doesn't override the product's existing collector-first default — it's additive emphasis, not a reversal.

### 3. Headwinds / blockers
- No spec or roadmap entry backs the nav-order change above — it shipped as a direct code commit outside the spec → cto → backend/frontend path. Not blocking anything today, but worth a product decision on whether "Shop first for signed-in users" is an intentional storefront-promotion move or a one-off; nobody has stated it as policy.
- No other blockers this period — `git status --short` clean, single commit in window.

### 4. Learnings
- None this period beyond the above: the codebase saw exactly one commit in twelve days, confirming the storefront and collector are both in a quiet/pre-traffic state rather than actively iterating.

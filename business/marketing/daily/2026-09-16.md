## Marketing — 2026-09-16

### 1. Progress since the last report
- Commits since last report — was 0, now 1 (`9c1e53e`) — `git log --oneline --since="2026-09-15" origin/master`.
- `brand.md` — still does not exist — `find business -iname brand.md` returns nothing; unchanged since `business/daily/2026-09-15.md` (reports branch), which reported the same absence.
- Order/revenue/signup counts — not available in this environment (no access to `pokewatch.db`, which is gitignored and lives only on the droplet); unchanged caveat from prior reports.

### 2. Highlights
- `9c1e53e` "Move Shop to the left of the nav bar" — reorders the signed-in nav so the Shop tab (tcgoftexas.com) now renders first in `TabNav.jsx` — verified via `git show --stat 9c1e53e`: `src/components/TabNav.jsx | 2 +-`, 1 insertion/1 deletion. Verified independently, not just from the commit message: the `/` landing tab is unaffected for signed-in users — `tabFromPath()` in `src/App.jsx:47-51` falls back to `'portfolio'` for any path not in `TAB_PATHS`, including `/`. No storefront copy, price, or feature claim changed by this commit.

### 3. Headwinds / blockers
- **`brand.md` still absent** — no voice-of-record file exists to draft campaign copy against; blocks any new copy beyond what has already shipped. Not blocked on anyone else; carried forward from the 2026-09-15 report.
- **No campaign assigned this window.** Blocked on a human decision.
- Any pricing or card-fact copy still needs to route through `cfo` (`products`/`orders` tables) or the `catalogue-sync` ground rules respectively — moot this window since no new asset was drafted.

### 4. Learnings
- `9c1e53e` puts Shop ahead of Portfolio in the signed-in nav, which changes discoverability of tcgoftexas.com among existing PokéWatch users. I have no click-through or order data in this environment to say whether that helps — those counts are not available here. Recording this as an open question for `cfo` once order data is reachable, not as a claimed win.

### 5. Skill and agent updates
**Automated:** none — `git log --oneline --since="2026-09-15" origin/master -- .claude/` is empty.
**Proposed:** none new this window.
